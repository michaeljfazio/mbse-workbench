# STATUS

## Current phase
phase:15 — Architect-driven UX & feature hardening

🎯 **Iter-898: PR-sweep iteration — PR #572 (iter-897 STATUS sync) auto-merged into main (`9872a04`); PR #571 (dim-17 inspector edge-style pickers) rebased onto new main to clear DIRTY state.** At iter-898 launch the open-PR set was `[#572 (BLOCKED + MERGEABLE, CI IN_PROGRESS, auto-merge SQUASH enabled), #571 (DIRTY, CONFLICTING)]` — #572 was left per AGENT.md "CI IN_PROGRESS → leave it" and auto-merged during iter-898 execution; #571 needed rebase per AGENT.md "DIRTY → resolve on the PR branch, push, let CI re-run." Cause of DIRTY: PR #571 was opened off `f4683ce` at iter-897, but #570 (edge-reconnect fix-ups) merged at `25bb92e` afterwards, and `src/workspace/store.ts` + `docs/architect/in-flight.md` were both touched on both branches. Rebase ran in the existing locked worktree `.claude/worktrees/agent-ae3258d1461c9d6f2` (left from iter-897's engineer subagent). 31 files auto-merged cleanly — only `docs/architect/in-flight.md` conflicted (both branches added stale claim-board rows). Resolution: kept only the single live row for `phase-15/dim17-inspector-edge-style-pickers`; all four sibling rows (`walk-36-execute`, `dim17-edge-reconnect`, `dim17-edge-reconnect-fixups`, plus the duplicate live row) refer to already-merged PRs. Process error: my first conflict-resolution Write tool call wrote to the primary worktree's `in-flight.md` absolute path, but the rebase was running in the linked worktree's separate copy — so `git add && git rebase --continue` from inside the worktree accepted the worktree's still-conflicted file and committed the `<<<<<<<` / `=======` / `>>>>>>>` markers into the rebased commit. Caught post-push by inspecting `git show HEAD:docs/architect/in-flight.md`. Fixed with follow-up commit `dac4da7` (`fix(docs): scrub leftover rebase conflict markers from in-flight.md (Refs #564 #566)`) — fast-forward push (no force). Local verification on the rebased branch: `tsc --noEmit` PASS, `tsc -b` PASS, `vitest run` 1533/1533 PASS (9 more than iter-897's 1524 because #570's reconnect tests are now also included on the branch's base), `vite build` PASS. PR #571 state at iter-898 close: `mergeable: MERGEABLE`, `mergeStateStatus: BLOCKED` (CI required, IN_PROGRESS on `dac4da7`), `autoMergeRequest.mergeMethod: SQUASH` (still enabled — didn't need re-enabling). Will auto-merge on green CI.

🎯 **Iter-897: engineer hat → second dim-17 engineer batch shipped — #564 (PC4 routing-style picker) + #566 (PC6 stroke/color picker STRICT) on a single themed PR #571.** Sonnet engineer subagent + Sonnet pre-PR review subagent (this time dispatched BEFORE `gh pr merge --auto` per the iter-896 retro lesson — auto-merge can fire faster than CI on green for short pipelines, which would have shipped review findings as fix-up commits rather than in the same PR). Branch `phase-15/dim17-inspector-edge-style-pickers` (off main `f4683ce`). Extensions: `EdgeBase` and the element-as-edge kinds (`ConnectionUsage`/`ItemFlow`/`Transition`) gain optional `routingStyle: 'straight' | 'step' | 'smooth-step' | 'bezier'` + `strokeStyle: 'solid' | 'dashed' | 'dotted'` + `strokeColor: string`. Defaults preserve current behavior (BDD/Requirements/Use Case/Package → smooth-step solid; IBD/Activity/State Machine → bezier solid; Dependency/RequirementTrace/Include/Extend/PackageImport → dashed). Threaded through all 17 viewpoint edge components via a single shared helper `src/viewpoints/shared/edgePath.ts`. Three new typed store actions (`setEdgeRoutingStyle`/`setEdgeStrokeStyle`/`setEdgeStrokeColor`) each dispatch through the command bus with a working `invert` so Cmd-Z restores prior value exactly. Inspector single-edge selection gains a routing-style segmented control (`[data-testid="edge-routing-style"]` with `{straight,step,smooth-step,bezier}`), a stroke-style segmented control (`[data-testid="edge-stroke-style"]` with `{solid,dashed,dotted}`), and a color swatch (`[data-testid="edge-stroke-color"]`). **Pre-PR review surfaced 3 IMPORTANTs (no BLOCKERs)** — 2 unused imports + missing visual baselines — all addressed in fix-up commit `9c40b2d` BEFORE PR open: dropped unused `BDD_ASSOCIATION_EDGE_TYPE`/`BDD_GENERALIZATION_EDGE_TYPE` imports (the shared Generalization/Association inspector panels use `USE_CASE_*` constants for both, and the routing/stroke defaults coincide for both viewpoints); added two new `@visual` baselines `bdd-composition-straight.png` + `bdd-composition-dashed.png` for chromium + webkit (additions only, no existing baselines modified). Final: typecheck PASS, lint PASS, vitest 1524/1524 PASS, build PASS, e2e 24/24 PASS. PR #571 open with auto-merge SQUASH enabled. **SECOND dim-17 PC-level unlock landed** — #562 (PC1 reconnect) shipped iter-896, #564 + #566 (PC4 + PC6 STRICT) shipping iter-897. PR #570 (iter-896 fix-ups) merged to `25bb92e` during iter-897 launch. In-flight 1 / 5 of A.8 cap.

🎯 **Iter-896: engineer hat → #562 (PC1 endpoint reconnect) shipped via #568 + code-review fix-ups via #570 — FIRST dim-17 PC-level unlock landed.** Single-iteration engineer batch on `phase-15/dim17-edge-reconnect` (Sonnet impl + Sonnet pre-PR review per A.8): wires React Flow v12 `edgesReconnectable` + `onReconnect{Start,End}` on the primary `<ReactFlow>` in `src/workspace/CanvasPane.tsx`; adds a typed `reconnectEdge` store action covering both `ModelEdge`-backed flow edges (`Composition`/`Aggregation`/`Generalization`/`Association`/`Dependency`/`RequirementTrace`/`PackageImport`) and element-as-edge kinds (`ConnectionUsage`/`ItemFlow`/`ControlFlow`/`ObjectFlow`/`Transition`/`ParameterBinding`/`UseCaseEdge`); all viewpoint validators (`isValidBddConnection`/`isValidIbdConnection`/`isValidActivityConnection`/`isValidStateMachineConnection`/`isValidUseCaseConnection`/`isValidParametricConnection`/`isValidPackageConnection`) gate the new endpoint pair; `invert` restores both endpoints atomically for Cmd-Z. 9 unit tests + 4 Playwright e2e tests in #568. **Pre-PR review caught 2 blockers post-merge** (auto-merge fired on green CI before review report landed): (B1) `Generalization`/`Association` ModelEdges in #568 routed unconditionally to `isValidBddConnection`, silently rejecting valid UseCase Actor↔Actor reconnects — fixed by branching on active-diagram viewpoint id in `reconnectEdge`; (B2) `newHandleId == null` on `ConnectionUsage`/`ItemFlow` bypassed the validator — fixed to no-op with `false`; (I1) `end`-detection mis-classified same-node IBD handle swaps — fixed to compare both node id AND handle id; (I-extra) transposed `sourceHandle`/`targetHandle` in the IBD synthetic-connection builder discovered while writing tests — fixed; 9 new unit tests added for the IBD ConnectionUsage/ItemFlow paths + same-node handle swap regression test. Fix-ups landed as **#570** (auto-merge SQUASH pending CI) on `phase-15/dim17-edge-reconnect-fixups` (cherry-picked off main, not on the original branch — the merged squash of #568 made the original branch DIRTY for stacked work). PR #569 closed as stale (the same-branch attempt that hit the DIRTY state). In-flight 1 / 5 of A.8 cap.

🎯 **Iter-895: architect hat → walk-36 EXECUTED → row 3 (3–6 PCs FAIL — expected outcome).** Walk-36's dedicated dim-17 (edge editing affordances) deep-dive ran against the byte-identical `vphase-15.10` / `v1.6.1` Pages bundle (re-verified `last-modified: Mon, 18 May 2026 23:11:59 GMT`, `etag: "6a0b9cbf-1eb"` at driver launch). Driver: `artifacts/phase-15/walk-36/walk-36-exec.py` (gitignored). Outcome: **2/20 PASS (PC7 zero-errors cross-cutting + PC8 persistence cross-cutting), 18/20 FAIL (BDD + IBD + Activity × 6 affordances)**. 100% pre-plan-seal prediction accuracy — zero surprises; the walk was measurement, not discovery. **Five `type:feature` issues filed** per A.7 one-issue-per-defect (with cross-viewpoint workbench-wide misses captured as `area:cross-cutting` rather than per-viewpoint duplicates): `#562` (PC1 reconnect, p1, `area:cross-cutting`+`area:interaction`), `#563` (PC2/PC3 waypoint editing, p2, `area:routing`+`area:interaction`), `#564` (PC4 routing-style picker, p2, `area:routing`+`area:inspector`), `#565` (PC5 label drag, p2, `area:routing`+`area:interaction`), `#566` (PC6 stroke/color picker STRICT, p2, `area:cross-cutting`+`area:inspector`). **Chain resets 1 → 0 / 3** per walk-36.md § Decide next; **dim 17 stays at 2** with PC-level evidence now appended to `docs/architect/quality-rubric.md` row 17 + five filed issues as explicit promote pre-requisites. JOURNAL entry appended (`event: escalation`, precedent: iter-889 walk-34 chain reset). Closes the architect-walk lifecycle; engineer batches begin iter-896+.

🎯 **Iter-894: architect hat → walk-36 PLAN-SEAL.** Walk-36 plan authored at `docs/architect/walks/walk-36.md` — dedicated **dim-17 (edge editing affordances) deep-dive** against `vphase-15.10` / `v1.6.1` Pages (chain[2] candidate). Plan-seal PR also appended `## 2026-05-19 — Dim-17 edge-editing affordance conventions (walk-36 prereq)` research section to `docs/architect/visual-standards.md` (primary-source-cited per A.9 generalised to cross-cutting dims). Closes `#560` (merged at `5392a6c`, PR #561).

🎯 **Iter-893: architect close-out for walk-35 — chain advance 0 → 1 / 3 + dim 10 (Use Case SysML conformance) promoted 2 → 3 (FOURTH score-3 dim) + JOURNAL `event: design-decision` entry per A.14 broader-interpretation precedent.** Closes `#556` (PR #558 merged at `24b5f6e`).

🎯 **Iter-892: walk-35 EXECUTED CLEAN against `vphase-15.10` / `v1.6.1` Pages.** 23/24 PASS + 1 INFO — row 1 of walk-35.md § Acceptance / rubric impact.

🎯 **Iter-891: walk-35 PLAN-SEAL with corrected use-case V-B driver.** Closed `#554`; merged at `4eff517` (PR #555).

🎯 **Iter-890: engineer hat → #548 closed as walk-driver artefact (NOT an application bug).** PR #553 lock-in e2e at `tests/e2e/use-case-edges.spec.ts`.

🎯 **Iter-889: walk-34 architect close-out — JOURNAL escalation entry + STATUS sync (#552 merged at `8b00915`).**

🎯 **Iter-888: walk-34 EXECUTED → 22/24 PASS + 1 PARTIAL (use-case/V-B) + 1 INFO (X-7); #548 filed (later resolved as driver artefact at iter-890).**

🎯 **Iter-887: walk-34 plan-seal merged at `a8651bb` (PR #547).**

🎯 **Iter-886: `vphase-15.10` / `v1.6.1` released — FIRST agent-cut release under ADR 0017.**

🎯 **Iter-885: `#542` filed + resolved via ADR 0017 in the same iteration.**

🎯 **Iter-879: engineer hat → #528 fix shipped (PR #531).** Actor source handles on `Position.Right` and `Position.Bottom`.

🎯 **Iter-826: rubric dim 5 (BDD) at score 3 = FIRST score-3 dimension.**
🎯 **Iter-834: rubric dim 14 (Round-trip integrity) → score 3 via SysML view-block round-trip (#451) = SECOND score-3 dimension.**
🎯 **Iter-867: rubric dim 6 (IBD) → score 3 via walk-30 clean regression on `vphase-15.8` Pages = THIRD score-3 dimension.**
🎯 **Iter-893: rubric dim 10 (Use Case SysML conformance) → score 3 via walk-35 clean regression on `vphase-15.10` Pages with corrected non-artefactual driver = FOURTH score-3 dimension.**

## Phase 15 termination conditions

| # | Condition | Status |
|---|-----------|--------|
| A.12 #1 | Every rubric dim at 3 | **4 of 28** at 3 (dim 5 BDD, dim 14 Round-trip, dim 6 IBD, dim 10 Use Case SysML); 21 at 2; 0 at 1; 3 at 0. Unchanged at iter-895. |
| A.12 #2 | Zero open `phase:15` issues labelled `type:bug/feature/design` | **Un-satisfied at iter-897 close** — 2 open (#563/#565). #564 + #566 closing via #571. Iter-898+ continues the engineer-batch sequence (third dim-17 batch: #563 waypoint editing + #565 label drag on shared per-edge `data` extension). |
| A.12 #3 | Three consecutive convergence walks | **chain[0 / 3]** at iter-895 close, unchanged at iter-896 and iter-897 — engineer batches don't advance the chain (walks do). chain[1] candidate is the post-issue-closure regression walk on a future `vphase-15.N` bundle once all five dim-17 fixes have shipped. |
| A.12 #4 | FBW example shipped + loadable | **Un-gates on chain saturation + rubric saturation.** |

## Current iteration
- Iteration #: 898
- Started: 2026-05-19 (UTC, post-#572 auto-merge at `9872a04`)
- Branch: `phase-15/iter-898-rebase-571` (off main `9872a04`) — STATUS sync chore PR for the iter-898 PR-sweep work
- Working on: STATUS sync for iter-898's PR-sweep iteration (rebased PR #571 onto main + conflict-marker fix-up commit `dac4da7`)

- Post-rebase local verification on `phase-15/dim17-inspector-edge-style-pickers` at `dac4da7` (in the linked worktree at `.claude/worktrees/agent-ae3258d1461c9d6f2`): `tsc --noEmit` PASS, `tsc -b` PASS, `vitest run` **1533/1533 PASS** (9 more than iter-897's 1524 because the branch now includes #570's reconnect-fix-up tests as a base), `vite build` PASS.
- e2e not re-run locally on iter-898 — the changes between iter-897's last green check (`9c40b2d`) and iter-898's `dac4da7` are: (a) auto-merge of #570's `store.ts` and `reconnectEdge.test.ts` changes into the rebased commit (a known-good base on main), and (b) the in-flight.md conflict-marker scrub (docs-only). Neither touches code under test paths beyond what unit tests cover. CI will re-run the full e2e suite on the PR.
- CI on PR #571 IN_PROGRESS (CI run started at 02:47:49 for the rebased commit `312c2c0`; expected to re-trigger for the conflict-marker fix-up `dac4da7`); auto-merge SQUASH still enabled.

## Last health check

Per AGENT.md directive #13, **iter-880** ran the periodic health check — all four checks PASS. Next health check is **iter-900**. Iter-895 incidentally re-verified Pages reachability + byte-identical headers — passes — but does not advance the formal cadence.

## Last PR sweep
- Iter-898 launch: `gh pr list --state open` returned `[#572 (BLOCKED + MERGEABLE, CI IN_PROGRESS, auto-merge SQUASH enabled), #571 (DIRTY, CONFLICTING)]`. #572 left per "CI IN_PROGRESS → leave it" and auto-merged into main (`9872a04`) during iter-898 execution. #571 rebased onto new main per "DIRTY → resolve on the PR branch, push, let CI re-run" — `git rebase origin/main` on the linked worktree, 31 files auto-merged, 1 conflict in `docs/architect/in-flight.md` resolved by keeping only the single live row. Force-pushed (`+ 9c40b2d...312c2c0 ... (forced update)`); follow-up commit `dac4da7` fast-forward pushed to scrub conflict markers that had been committed in `312c2c0` due to a worktree-path mix-up. **In-flight 1 / 5 of A.8 cap** at iter-898 close (#571 only).
- Iter-897 launch (preserved): `gh pr list --state open` returned `[#570 (BLOCKED + MERGEABLE, CI IN_PROGRESS, auto-merge SQUASH enabled)]` — left per "CI IN_PROGRESS → leave it." #570 auto-merged to `25bb92e` during iter-897 execution. Iter-897 opened PR #571 (`feat(canvas,inspector): per-edge routing-style and stroke-style/color pickers`, closes #564 + #566).

## Known issues / blockers
- None. The 2 remaining dim-17 issues (#563/#565) are the *expected* iter-899+ engineer-batch backlog (third dim-17 batch grouping waypoint editing + label drag on the shared per-edge `data` extension introduced by #571). The pre-existing webkit a11y color-contrast flake on the toolbar undo button persists across iters and is not caused by this iteration's changes; it is its own dim-25 finding and will be addressed when dim-25 is the active focus.

## Open phase:15 issues at iter-898 close (after #571 merges)
- **2 `type:feature` (`status:ready`):** #563 (p2, waypoint editing), #565 (p2, label drag).
- **0 `type:bug` / `type:design` open.**

## Decisions log

**Iter-808..iter-895 entries preserved in earlier commits.**

- **Iter-898 — PR-sweep iteration: PR #571 rebased onto main (`9872a04`) + worktree-path mix-up caught and fixed.** PR #572 (iter-897 STATUS sync) auto-merged into main during iter-898 launch. PR #571 went DIRTY because the iter-896→iter-897 sequence merged #568 then #570 then #572 onto main while #571 was open off `f4683ce`. Rebase was straightforward at the code level (31 files auto-merged cleanly, including the `src/workspace/store.ts` overlap between #570's `reconnectEdge` action and #571's three style-setter actions). The only conflict was in `docs/architect/in-flight.md` where both branches added stale rows — resolution: keep only the single row for the currently-in-flight PR.

- **Iter-898 — worktree-path mix-up lesson learned (LOOP-WIDE).** When resolving a rebase conflict using the Write tool, the absolute file path passed to Write must match the worktree where `git rebase` is running. The conflict-resolution Write call used `/Users/michaelfazio/Source/mbse-workbench/docs/architect/in-flight.md` (the PRIMARY worktree), but the rebase was running in the linked worktree at `/Users/michaelfazio/Source/mbse-workbench/.claude/worktrees/agent-ae3258d1461c9d6f2/` — each worktree has its own physical filesystem files for tracked paths, so the resolution landed in the wrong working tree and the rebase committed the unresolved conflict markers from the worktree's still-conflicted file. Caught post-push by `git show HEAD:docs/architect/in-flight.md` showing `<<<<<<<` markers. Fixed in a follow-up commit `dac4da7` rather than amending — fast-forward push, simpler and squash-merges to the same end state. **For future iterations:** when working in a linked worktree, ALWAYS pass the worktree-prefixed path to Write/Edit, or `cd` into the worktree before invoking the tool. The Bash tool's cwd persistence does not auto-route Write/Edit absolute paths to the matching worktree.

- **Iter-898 — engineer-batch next-up: #563 + #565 (waypoint editing + label drag) [iter-899].** Unchanged from iter-897's plan — once #571 auto-merges, iter-899 dispatches `phase-15/dim17-edge-waypoints-and-label-drag` per the iter-897 § Decisions log sketch. Branch will extend the per-edge `data` infrastructure introduced by #571: waypoint editing adds `waypoints: { x: number; y: number }[]` to `EdgeBase` + element-as-edge types; label drag adds `labelOffset: { dx: number; dy: number }`. The engineer subagent should be briefed to study iter-897's `src/viewpoints/shared/edgePath.ts` helper as the integration point for waypoint-aware path computation.

- **Iter-897 — #564 (PC4 routing-style picker) + #566 (PC6 stroke/color picker STRICT) shipped via #571.** Second dim-17 PC-level unlock landed. Implementation by Sonnet engineer subagent (worktree-isolated), pre-PR review by Sonnet code-review subagent **before** the PR was opened (per iter-896 retro lesson — auto-merge can fire faster than the reviewer reports on short pipelines). Review returned APPROVE_WITH_FIXUPS with 3 IMPORTANTs (no BLOCKERs); all 3 addressed in fix-up commit `9c40b2d` BEFORE PR open: drop two unused imports in `Inspector.tsx`, drop one unused `prior` in a test file, add the two missing `@visual` baselines (`bdd-composition-straight.png` + `bdd-composition-dashed.png` for chromium + webkit). PR #571 contains both commits with `Closes #564` + `Closes #566`. Branch `phase-15/dim17-inspector-edge-style-pickers` is independent of iter-896's `phase-15/dim17-edge-reconnect-fixups` (no file overlap; touched-files registry on `docs/architect/in-flight.md` clean per A.8 claim-board discipline).

- **Iter-897 — pre-PR review timing discipline validated.** The iter-896 retro identified that auto-merge can fire faster than the review subagent reports on short CI pipelines, which would push review findings to a fix-up PR rather than being absorbed in the same PR. Iter-897 followed the corrected protocol: engineer subagent pushes branch (no PR yet) → main agent dispatches code-review subagent → main agent applies fix-ups on the same branch → main agent opens PR with `Closes` clauses → enable auto-merge. This kept all 3 IMPORTANTs in PR #571 as a single fix-up commit on the branch, avoiding the stacked fix-up-PR pattern that iter-896 used post-hoc.

- **Iter-897 — engineer-batch next-up: #563 + #565 (waypoint editing + label drag).** Per iter-896 § Decisions log sketch, the third dim-17 batch groups the per-edge waypoint editing (#563) and label drag (#565) on shared per-edge `data` extension infrastructure introduced by #571. Branch name: `phase-15/dim17-edge-waypoints-and-label-drag`. After #571's auto-merge fires, in-flight returns to 0/5, so iter-898 can dispatch immediately.

- **Iter-896 — #562 (PC1 reconnect) shipped via #568 + fix-ups via #570.** First dim-17 PC-level unlock landed. Implementation by Sonnet engineer subagent (single-shot, TDD: 9 unit + 4 e2e tests); pre-PR review by Sonnet code-review subagent caught 2 blockers + 4 importants AFTER #568's auto-merge had already fired on green CI. Fix-ups landed on a clean `phase-15/dim17-edge-reconnect-fixups` branch cherry-picked off main (`f4683ce`), not on the original branch (the merged squash made it DIRTY for stacked work). PR #569 (the stale same-branch attempt) closed as superseded; PR #570 is the live fix-up PR.

- **Iter-896 — A.5/A.8 pre-PR-review discipline saved a correctness bug.** B1 (`Generalization`/`Association` validator routing for UseCase Actor↔Actor reconnects) would have been a real silent rejection of valid user gestures had it not been caught by the review subagent. Lesson for the loop: dispatch the pre-PR review subagent *before* opening the PR (or before enabling auto-merge), not in parallel with CI — auto-merge can fire faster than the reviewer reports. For future iter-N engineer batches, the main agent should run the pre-PR review BEFORE `gh pr merge --auto`. Recorded for future iters; do not edit AGENT.md to add this — it's already implicit in A.5 "main agent reviews the diff via a pre-PR code-review subagent before opening the PR."

- **Iter-896 — engineer-batch next-up: #564 + #566 (Inspector edge-style pickers).** Per iter-895 § Decisions log sketch, the second batch groups the per-edge stroke/color picker (#566) and the routing-style picker (#564) on shared Inspector + per-edge `data` extension infrastructure. Branch name: `phase-15/dim17-inspector-edge-style-pickers`. Cap status remains 1/5 (only #570 in-flight) so iter-897 can dispatch the second batch immediately without queueing.

## Session checkpoint summary

This session (iter-793 → iter-898) has executed **106 iterations** spanning bootstrap, **21 broad/regression walks against deployed Pages** (walks 1 + 26..36), **~30 engineer batches** + **2 PR-sweep iterations** (iter-879 and iter-898), **10 release tags** (`vphase-15.1` → `vphase-15.10`), **4 ADRs** (0014/0015/0016/0017).

Most recent arc: iter-879 #528 fix shipped → iter-880 health check PASS → iter-881..884 four blocked-tick STATUS syncs → iter-885 ADR 0017 (agent-cut tags) → iter-886 vphase-15.10 / v1.6.1 first agent-cut release → iter-887 walk-34 plan-seal (PR #547) → iter-888 walk-34 execute (22 PASS + 1 PARTIAL + 1 INFO; #548 filed) → iter-889 walk-34 architect close-out (PR #552) → iter-890 engineer hat → #548 closed as walk-driver artefact + lock-in test (PR #553) → iter-891 walk-35 plan-seal with corrected driver (PR #555) → iter-892 walk-35 EXECUTE → CLEAN row 1 (23/24 PASS + 1 INFO) → iter-893 walk-35 close-out → chain 0 → 1 / 3 + dim 10 → 3 (FOURTH score-3 dim) + JOURNAL entry committed (PR #558) → iter-894 walk-36 plan-seal — dedicated dim-17 deep-dive against `vphase-15.10` Pages with primary-source-cited convention research + STRICT-reading PC6 + pre-plan-seal code scan documenting 5/5 sub-criteria not wired (PR #561) → iter-895 walk-36 EXECUTED → row 3 (2 PASS + 18 FAIL); chain resets 1 → 0; five `type:feature` issues filed #562–#566 (PR #567) → iter-896 engineer hat → #562 (PC1 reconnect) shipped via #568 + fix-ups via #570 — FIRST dim-17 PC-level unlock → iter-897 engineer hat → #564 (PC4 routing-style picker) + #566 (PC6 stroke/color picker STRICT) shipped via #571 — SECOND dim-17 PC-level unlock + pre-PR review timing discipline validated (review BEFORE auto-merge enable, fix-ups absorbed in same PR rather than stacked PR) → **iter-898 PR-sweep iteration: PR #572 auto-merged into main (`9872a04`); PR #571 rebased onto new main to clear DIRTY + conflict-marker fix-up `dac4da7` shipping a worktree-path-mix-up loop-wide lesson learned; 2 dim-17 issues remain open (#563/#565) for iter-899+**.

| Tag | Date | What |
|-----|------|------|
| vphase-15.1 / v1.1.0 | 2026-05-16 | Bootstrap + BDD resize |
| vphase-15.2 / v1.2.0 | 2026-05-17 | Drag-coord overlay + Cmd-Z rename fix |
| vphase-15.3 / v1.2.1 | 2026-05-17 | Palette show-all-kinds + label consistency |
| vphase-15.4 / v1.3.0 | 2026-05-17 | IBD palette, usage hints, implicit-owner Package row, bus-dispatched diagrams |
| vphase-15.5 / v1.4.0 | 2026-05-17 | ADR 0015 steps 2/3/4 |
| vphase-15.6 / v1.5.0 | 2026-05-18 | #448 quoted-ident + #451 SysML view-block round-trip → dim 14 to 3 |
| vphase-15.7 / v1.5.1 | 2026-05-18 | #464 IBD enclosing-frame seed + #465 tree-row activates diagram tab |
| vphase-15.8 / v1.5.2 | 2026-05-18 | #499 IBD `ConnectionMode.Loose` for inout↔inout drag + #500 acronym auto-name |
| vphase-15.9 / v1.6.0 | 2026-05-18 | #517 Actor↔UseCase Association + ADR 0007 § 5/§ 7 deferral closure |
| vphase-15.10 / v1.6.1 | 2026-05-19 | #528 use-case bidirectional-drag fix (#531) + ADR 0017 agent-cut tag cadence (#543). Walk-35 (iter-892) → dim 10 to 3 at iter-893. Walk-36 (iter-895) → 5 dim-17 issues filed #562–#566. |

Rubric: **4 × score-3** (dim 5 BDD, dim 14 Round-trip integrity, dim 6 IBD, dim 10 Use Case SysML conformance) + **21 × score-2** + **0 × score-1** + **3 × score-0** (dims 3, 11, 23). Unchanged from iter-894 close.

## Next action

**Iter-899 — engineer hat → dispatch the third (and final) dim-17 engineer batch grouping #563 (PC2/PC3 waypoint editing) + #565 (PC5 label drag) on `phase-15/dim17-edge-waypoints-and-label-drag`.** Both features extend the per-edge `data` infrastructure landed by #571 (iter-897) — waypoint editing adds `waypoints: { x: number; y: number }[]` to `EdgeBase` + element-as-edge types; label drag adds `labelOffset: { dx: number; dy: number }` (or equivalent). Inspector + canvas surface controls (right-click context menu for insert/remove waypoint per #563 acceptance criteria; drag-handle on the edge label per #565). Per A.8 soft cap (5), iter-899's dispatch is permitted in parallel with #571's CI tail if the latter hasn't yet auto-merged.

**Iter-899 process discipline (reinforced from iter-897, iter-898):**
1. Pre-PR review subagent dispatched BEFORE PR open (not just before auto-merge enable) — strict serialisation: engineer subagent pushes branch → code-review subagent reports → fix-ups applied → THEN `gh pr create` → THEN `gh pr merge --auto --squash`. Iter-897 validated: 3 IMPORTANTs absorbed in the same PR as a single fix-up commit.
2. When working in a linked worktree, ALWAYS pass the worktree-prefixed absolute path to Write/Edit, or `cd` into the worktree before invoking. The Bash tool's cwd persistence does not auto-route Write/Edit absolute paths to the matching worktree (iter-898 worktree-path mix-up lesson).
3. After a rebase, verify the committed file content with `git show HEAD:<path>` for any file that had a conflict, before pushing. `git rebase --continue` will happily accept a still-conflicted file if it has been staged.

**Iter-899 process discipline reminder (carried from iter-896):** Per A.5, the main agent's role is reconciliation + review. Engineer batches go to Sonnet subagents in isolated worktrees; code review goes to a separate Sonnet subagent reading the diff. The main agent does NOT implement features — it briefs subagents, reviews their work, opens PRs.

**Iter-899 setup for #563 + #565:** the engineer subagent should be briefed to study iter-897's `src/viewpoints/shared/edgePath.ts` helper as the integration point for waypoint-aware path computation, and the iter-897 Inspector edge-style controls as the styling template for label-drag UX. React Flow v12 native waypoint support uses the `<BaseEdge>` `path` prop directly — the helper should compute a piecewise path through the waypoint list when present.

**Halting safety:** STOP file / `status:emergency-stop` label unchanged; Phase-15 iter-count at 106, well under the 300 churn ceiling.

**In-flight at iter-898 close (1 / 5 of A.8 cap until PR #571 merges, then 0 / 5):**
- `phase-15/dim17-inspector-edge-style-pickers` (PR #571) — rebased onto main (`9872a04`) + conflict-marker fix-up `dac4da7`; auto-merge SQUASH pending CI re-run.
