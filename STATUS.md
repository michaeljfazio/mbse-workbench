# STATUS

## Current phase
phase:4 — Requirements Diagram (Phase 3 closed; vphase-3 deployed)

## Current iteration
- Iteration #: 30
- Started: 2026-05-12
- Branch: status/iteration-30-requirement-node (PR #78 merged at 495260e)
- Working on: status update — #71 shipped, picking up #72 next iteration

## Last test run
- Command: CI run 25696610065 on issue/71-requirement-node
- Result: PASS (168 tests, 1.9 min — chromium + webkit + visual + a11y)
- Failures: (none — added 26 new tests: 16 unit + 10 e2e × 2 browsers; 8 baselines committed)

## Known issues / blockers
- (none)

## Decisions log
- 2026-05-11: Bootstrap as a single committed scaffold, not iterative through child PRs. Reason: AGENT.md Phase 0 explicitly lists scaffold steps as the bootstrap and instructs iteration 1 to "run Phase 0 bootstrap" when STATUS.md is missing; opening child issues against an empty repo with no CI yet would be the wrong order. Child issues for any *remaining* Phase 0 polish are opened after the initial commit.
- 2026-05-11: Pinned minor versions per AGENT.md "Version pinning" rule. React 18.3.x, Vite 5.4.x, Tailwind 3.4.x, @xyflow/react 12.3.x, Vitest 2.1.x, Playwright 1.48.x, Anthropic SDK 0.32.x, ESLint 8.57.x. Tailwind 3 (not 4) avoids the PostCSS landmine.
- 2026-05-11: `pnpm run check` composes typecheck + lint + unit + build + e2e in that order; the Playwright suite is the single source of truth for functional, visual (`@visual`-tagged) and accessibility (`@a11y`-tagged) specs across `chromium` and `webkit` projects.
- 2026-05-11: Repo name fixed to `mbse-workbench`; Pages base path `/mbse-workbench/` in production via `vite.config.ts`.
- 2026-05-11: Allow `vphase-*` and `v*.*.*` tags to deploy to the `github-pages` environment via environment deployment-branch-policies (type: tag). Reason: protection rule blocked the first vphase-0 deploy with a tag policy error; documented in `docs/CONTEXT.md` so future phases skip this.
- 2026-05-11: Phase 1 decomposed into 5 child issues (#17 metamodel, #18 registry, #19 command bus, #20 repository, #21 collaboration seams). Reason: AGENT.md Ralph loop step 6 — just-in-time decomposition when the phase becomes current. Linked from epic #2's task list.
- 2026-05-11: Metamodel split — 19 element kinds in `ModelElement` discriminated union; 9 pure structural relationships in `ModelEdge`. `ConnectionUsage` / `ItemFlow` / `Transition` are elements (named, selectable) with `sourceId`/`targetId` baked in; the diagram renderer derives edge geometry from either source. Recorded in [ADR 0002](docs/adr/0002-metamodel-shape.md) (links: #17).
- 2026-05-11: Registry `update<K>` replaces the stored element with a new object rather than mutating in place, so a `readonly ModelElement[]` snapshot taken before `update` is unaffected. Reason: command-bus undo/redo and future event-sourcing will rely on snapshot stability; in-place mutation would silently corrupt history. Runtime kind-check guards the case where the caller-chosen `K` does not match the stored element's `kind` (TS cannot infer K from a runtime id). Records dangling refs for the three element-kinds that carry `sourceId`/`targetId` directly (links: #18).
- 2026-05-11: Command bus shape — `Command` is a discriminated union of `create-element` / `update-element` / `delete-element` / `link` / `unlink` / `compound`. Generic over `ElementKind` and `EdgeKind` rather than 75 per-kind command types, because the metamodel is already a discriminated union and the registry validates kind via the existing `update<K>` runtime guard. ModelEvent payload = inverse command, so the append-only event log is self-undoable. Undo/redo stacks hold `{ forward, inverse, actor }` and just `applyOnly` (no re-capture). Delete-element's inverse is a `compound` of `create-element` + one `link` per incident edge captured at apply time (links: #19).
- 2026-05-11: Repository is a thin async port: one key per project at `mbse:v1:project:<id>`, ISO-string dates, `ProjectNotFoundError` covers both missing-key and malformed-JSON cases (caller treats them identically), `list()` silently skips malformed entries so one bad entry does not blind the picker, and `StorageQuotaError` wraps the underlying `QuotaExceededError` (and Firefox's `NS_ERROR_DOM_QUOTA_REACHED`). `Project` keeps `elements` / `edges` as readonly arrays so the consumer cannot mutate the cached value behind the registry's back (links: #20).
- 2026-05-11: Collab module split per responsibility (`user` / `presence` / `provider` / `permissions`) instead of one `types.ts` grab-bag. `User = { id, displayName, color }`; deterministic per-session color picked from `USER_COLORS` by hashing the id so the same id always renders identically across reloads. `can` (single-user default) returns true unless `target.ownerId` differs from `user.id`, giving Phase 1 a real ownership seam even though single-user mode never sets `ownerId`. Command bus accepts `provider?: CollaborationProvider` (defaults to Noop) and publishes every committed dispatch/undo/redo event post-apply — denied permissions throw *before* publish, so the provider only ever sees committed events (links: #21).
- 2026-05-11: Phase 1 closed and vphase-1 tagged at c1214db. Release workflow #25670869402 green; Pages HTTP 200; screenshot uploaded as `app-shell.png` release asset. Phase 1 shipped data layer only — deployed UI unchanged from vphase-0 (links: #27).
- 2026-05-11: Phase 2 decomposed into 7 child issues (#30 workspace shell + viewpoint registry, #31 BDD React Flow canvas + Block/Composition/Generalization, #32 inspector, #33 project tree + drag-from-tree, #34 dagre auto-layout + per-view positions, #35 PNG/SVG export, #36 Phase 2 gate e2e). Reason: AGENT.md Ralph loop step 6 — just-in-time decomposition. Sequencing: #30 unblocks #31; #31 unblocks #32-#35 (independent of each other); #36 depends on all. Linked from epic #3's task list (links: #29).
- 2026-05-11: `@xyflow/react` v12.3.x research findings recorded in `docs/CONTEXT.md` ahead of Phase 2 first use — Zustand wiring (drive via props + applyNodeChanges/applyEdgeChanges, route through command bus), required CSS import, `node.width`/`node.height` (not `style`), `nodrag`/`nopan` classes on interactive children, fresh `dagre.graphlib.Graph()` per layout call for strict-mode safety, built-in delete key. Reason: AGENT.md directive 11 — version pinning + library currency. Verified via context7-fetched authoritative v12 docs (links: #29).
- 2026-05-11: Workspace shell foundation (#30) committed at daa7c6f. Three-pane resizable layout (project tree / canvas / sidebar tabs), Zustand `useWorkspaceStore`, `Viewpoint<T>` registry with BDD stub, project bootstrap via `InMemorySessionRepository`, pane-width persistence to sessionStorage. Resize divider exposed as a keyboard-operable `role=separator` with `aria-valuenow`/`aria-controls`. PR #38 merged at 071eb3d.
- 2026-05-11: Visual snapshot baselines pinned to **Linux** renderer (CI runs ubuntu-latest). `playwright.config.ts` skips `@visual` specs when `!CI && platform !== 'linux'` so the agent's darwin `npm run check` doesn't flap on font hinting. Baselines live under `tests/e2e/__screenshots__/{testFileName}/{arg}-{projectName}.png` (via `snapshotPathTemplate`). Generation procedure: `mcr.microsoft.com/playwright:v1.48.2-jammy` via `podman --platform linux/arm64` + `vite preview` (Vite dev hits `ENOSPC` on file-watcher limits in the container). Full procedure recorded in `docs/CONTEXT.md` for future viewpoints (links: #30).
- 2026-05-11: `Viewpoint<T>` interface replaces placeholder `renderNode`/`renderEdge` with ReactFlow-native `nodeTypes` / `edgeTypes` (frozen module-scope) plus `nodeTypeFor(element)` / `edgeTypeFor(edge)` mappers. Reason: ReactFlow's typed custom-node/edge approach is the idiomatic seam; the placeholder methods were never used. Each viewpoint is now a single config registered via `viewpoints.register(...)` and `CanvasPane` consumes its `nodeTypes`/`edgeTypes` directly. Adding the next viewpoint (#33–#39 etc) requires zero core changes (links: #31).
- 2026-05-11: Per-view positions live on `Diagram` (`positions: Record<ElementId, {x,y}>`), not on the element. Reason: a single element instance must appear in multiple diagrams at different coordinates per the design directive. Moves go through `setNodePosition(diagramId, elementId, pos)` as a direct store mutation, NOT through the command bus — positions are presentation-only and shouldn't clutter the event log. Element create/delete/rename/link/unlink still go through the bus; deleting an element cascades the registry which automatically frees the position entry on the next snapshot refresh (links: #31). **Superseded 2026-05-11 by the #34 decision below.**
- 2026-05-11: Custom nodes must NEVER import `@/workspace/store` at module load. `BlockNode` originally did so for its `renameElement` call, which created a load-time cycle (store → viewpoints → BDD → BlockNode → store) that left `bddViewpoint` undefined when the store's singleton registry tried to register it. Solution: callbacks like `onRename` pass through ReactFlow node `data`. Pattern applies to every viewpoint. Recorded in `docs/CONTEXT.md` (links: #31).
- 2026-05-11: Edge markers (composition diamond, generalization triangle) are rendered as per-edge SVG `<marker>` defs inside each custom edge component, with unique ids `bdd-{kind}-{shape}-${edgeId}`. ReactFlow's built-in `MarkerType` only covers open/closed arrows, so we draw the diamond/triangle directly. `markerStart` for composition (on the *whole* side), `markerEnd` for generalization (on the *parent* side). `orient="auto-start-reverse"` keeps the marker oriented with the path direction (links: #31).
- 2026-05-11: Block-node label is a non-interactive `<div onDoubleClick=...>` rather than a `<button>`. Reason: ReactFlow already exposes the node wrapper as `role="button" tabindex="0"`, and nesting a `<button>` inside fails axe-core's `nested-interactive` rule (serious). Inline rename is still reachable via mouse double-click today; #32 (inspector) will provide the keyboard route via name editing in the inspector (links: #31).
- 2026-05-11: `vite.config.ts` honors `VITE_BASE_OVERRIDE` ahead of its `mode` check. Reason: `vite preview` re-reads the config to decide its mount path, so even an `--base=/` build still gets served at `/mbse-workbench/` under preview. The Linux baseline run-script sets `VITE_BASE_OVERRIDE=/` for both `vite build` and `vite preview` so `page.goto('/')` works. Production CI builds for Pages do NOT set the override (links: #31).
- 2026-05-11: Inspector (#32) ships the canvas → inspector → edit → reflect loop. New file `src/workspace/inspector/Inspector.tsx` renders empty / single / multi states; single-selection has `Name` + `Description` + read-only `Owner`. Edits commit on blur (Enter for name); empty names roll back. New store action `setElementDescription` dispatches `update-element` with a `documentation` patch (or `undefined` to clear). Workspace autosaves to the repository on every bus event so refresh persists. Registry `update<K>` guard whitelists optional ElementBase fields (`ownerId`, `documentation`) so first-time documentation edits don't trip the kind-mismatch check. Global Cmd/Ctrl+Z and Cmd/Ctrl+Shift+Z keyboard shortcuts route through the workspace store's undo/redo; suppressed inside text inputs so native input-undo handles in-place edits. The R/L Inspector exits to a multi-selection message when more than one element is selected — multi-edit is intentionally out of scope. All new behaviour recorded in `docs/CONTEXT.md` (links: #32, PR #40).
- 2026-05-11: PR #40 (inspector) needed a `gh pr update-branch --rebase` before auto-merge would fire — STATUS.md commit on main between PR open and CI completion put it in `mergeStateStatus: BEHIND` while branch protection has `strict: true`. Re-ran CI green (run 25676867834, 34.4 s e2e) and PR merged squash. Future iterations: if STATUS.md is pushed to main between PR-open and merge, expect a rebase round-trip. Merged at b102fff (links: #32, PR #40).
- 2026-05-11: **Position-as-command-bus** (#34) — `update-diagram-position` is now a first-class `Command` variant. The bus reads/writes positions via a new `DiagramPositionStore` port (`src/commands/diagramPositions.ts`); the workspace store implements it on a closure that mutates `diagrams[d].positions` via Zustand `set`. **Reverses the earlier #31 "presentation-only, no command-bus" decision** because issue #34 explicitly requires single-step undo for both manual drag and auto-layout. `createBlock` wraps create-element + initial position in a compound, so create-and-place is one undo step. `setNodePosition` short-circuits when the new position equals the old, so mouse-up-without-move drags don't pollute the undo stack. `delete-element` deliberately leaves the position entry in place so undo restores the element at its prior coordinates without needing extra inverse machinery — verified by `bddActions.test.ts`. `Project.diagrams` round-trips through `InMemorySessionRepository`; `load()` normalises missing `diagrams` to `[]` for forward-compat (links: #34).
- 2026-05-11: Auto-layout (#34) is a single bus dispatch of a compound command containing N `update-diagram-position` sub-commands (one per element returned by `dagreLayout`). One Cmd-Z reverts the whole layout. `dagreLayout(elements, edges, options)` builds a fresh `dagre.graphlib.Graph()` every call (strict-mode safety), returns `Map<ElementId, NodePosition>` translated from dagre's centre coordinates to React Flow's top-left. Default rankdir TB, nodesep 60, ranksep 80. Toolbar "Auto-layout" button calls `runAutoLayout(diagram.id)`; disabled when there are no elements (links: #34).
- 2026-05-11: Playwright drag tests of selected blocks fall over a strict-mode locator violation because the Inspector container also carries `data-element-id` for the currently-selected element. Fix: always scope by `[data-testid="bdd-block-${id}"]` rather than the bare attribute. Recorded in `docs/CONTEXT.md` (links: #34).
- 2026-05-11: Project tree (#33) — kind groups computed from union of (a) registry kinds and (b) `paletteItems[].elementKind` across all registered viewpoints; an empty BDD project still shows the "Blocks" affordance. `draggable` only when kind is in the *active* viewpoint's palette. Drag payload uses `application/x-mbse-element-kind` (`PROJECT_TREE_DRAG_TYPE`). Canvas drop validates against `acceptedElementKinds`, translates via `reactFlow.screenToFlowPosition`, centers on cursor, auto-selects new element. Roving-tabindex via derived focus key (`explicitFocusKey ?? visibleKeys[0]`) — no state-syncing useEffect. The pane wrapper now uses `aria-label="Project tree pane"` so the inner `role="tree"` keeps the canonical `Project tree` name that existing workspace-shell tests assert on (links: #33, PR #42).
- 2026-05-11: Export (#35) — `src/workspace/export/` ships a pure `buildDiagramSvg` (XML built from `{elements, edges, positions, viewpoint}`), a thin PNG rasteriser that hands the SVG to a `<canvas>` at 2× DPR, and a slug helper. The two exporters share `BuildDiagramSvgInput`, so PNG and SVG always agree on geometry. `mbse-node` / `mbse-edge` class names are the contract for the spec's structural assertions. Toolbar `ExportMenu` is a split-button: closes on outside-pointerdown / Escape; menuitems trigger download then close. Visual baselines pass unchanged on the new button — the diff is well under `maxDiffPixelRatio: 0.01`, verified by running the Linux-container @visual suite; rationale captured in `docs/CONTEXT.md` so future single-button toolbar additions don't trigger needless regenerations (links: #35).
- 2026-05-11: Iteration 17 — Drafted Phase 2 gate spec (#36) and discovered AGENT.md's "refresh → Cmd-Z → link gone" sequence is unsupported: the command bus's undo/redo stacks live in the bus's closure and are wiped on every bootstrap. Rather than reorder the gate (which would diverge from AGENT.md's verbatim wording) or fix it in #36's PR (would bundle two issues into one), opened **#44** ("feat(commands): persist undo/redo history across page reload") as a new Phase 2 child, marked #36 `status:blocked`, and implemented #44 this iteration. `Project` now carries `history: { undo: UndoEntry[]; redo: UndoEntry[] }`; `CommandBus` exposes `getHistory()` and accepts `initialUndoStack` / `initialRedoStack`. Legacy / malformed `history` fields normalise to `EMPTY_COMMAND_HISTORY` on load so older persisted entries don't break (links: #44, PR #45).
- 2026-05-12: Phase 2 closed and **vphase-2 tagged at 0f93af0**. Phase 2 gate spec (#36) merged via PR #46 (CI run 25681274560, 72/72 green chromium + webkit + visual + a11y). Release workflow 25681490005 (build + deploy-pages + github-release) green; Pages https://michaeljfazio.github.io/mbse-workbench/ returns HTTP 200; smoke walkthrough of the live URL via headless Chromium captured four screenshots (shell → +Block ×2 → Auto-layout → Inspector rename to "Engine") uploaded as release assets on vphase-2. First release that the demo URL actually demos something — vphase-0 and vphase-1 only shipped an app shell. Phase 3 (IBD) begins next iteration; Viewpoint registry already shaped so adding it means writing one folder + one config (links: #3 epic closed, #47 release, PR #46).
- 2026-05-12: Phase 3 decomposed into six child issues (#49 IBD viewpoint registration + ADR 0003 IBD shape, #50 PartUsage node + PortUsage handles + port management, #51 ConnectionUsage edge + typed compatibility, #52 ItemFlow edge variant, #53 cross-diagram navigation, #54 Phase 3 gate spec). Reason: AGENT.md Ralph loop step 6 — just-in-time decomposition. Sequencing: #49 unblocks #50; #50 unblocks #51/#52 (parallelizable); #53 independent of the rest; #54 depends on #49-#53. Linked from epic #4's task list (links: #48).
- 2026-05-12: `@xyflow/react` v12.3.x multi-handle-per-node research findings recorded in `docs/CONTEXT.md` ahead of Phase 3 first use — multiple `<Handle>` per side use unique `id` plus inline `style={{ top: '<pct>%' }}`, `Connection.sourceHandle`/`targetHandle` carry the handle id, no built-in label (use a sibling `<span>` or `<Handle>` children), `isConnectable` and `isValidConnection` compose independently. Reason: AGENT.md directive 11 — verify pinned API before first use. Verified via context7 (links: #48).
- 2026-05-12: IBD viewpoint (#49) — `ibdViewpoint` registered alongside BDD with empty `nodeTypes`/`edgeTypes` (frozen module-scope), `acceptedElementKinds: ['PartUsage']`, `acceptedEdgeKinds: []`, empty `paletteItems` (#50 introduces Part). `Diagram.context?: { kind: 'partDefinition'; id }` added per ADR 0003 and round-trips through `InMemorySessionRepository`; missing `context` normalises to `undefined` (forward-compat). New workspace action `createDiagram(viewpointId, options?)` appends and persists a diagram. `CanvasPane` hides the BDD-only "+ Block" toolbar button when the active viewpoint isn't BDD — IBD's toolbar still shows the viewpoint label and Auto-layout/Delete/Export (disabled until #50). Visual baseline `ibd-empty.{chromium,webkit}.png` committed (links: #49, PR #56).
- 2026-05-12: Axe `color-contrast` can sample tabs mid-CSS-transition immediately after a click and report a phantom contrast failure. Wait for `document.getAnimations()` `.finished` before `AxeBuilder.analyze()` in any `@a11y` spec that switches state right before the scan. Playwright's `animations: 'disabled'` only applies to screenshot capture, not axe-core. Discovered while landing #49; recorded in `docs/CONTEXT.md` (links: #49, PR #56).
- 2026-05-12: Iteration 21 — Shipped **#50** (PartUsage node + PortUsage handles + port management). Decisions: (a) added `PartUsageElement.portUsageIds: ElementId[]` to the metamodel so each PartUsage owns a stable list of PortUsages mirroring the type's `portIds` — required for ADR 0003's "handle id = PortUsage.id" rule (no metamodel link existed previously). (b) Drop on IBD opens a *"Type the part as…"* popover listing existing PartDefinitions; the chosen def becomes the new PartUsage's `definitionId`. Empty state instructs the user to create a PartDefinition first. (c) Inspector gains a `PartDefinitionExtras` section (Ports list + Add port + per-row name/direction/delete + Open Internal Diagram) and a `PartUsageExtras` section (read-only Definition + editable Multiplicity). (d) New workspace store actions: `addPortToDefinition` (compound), `deletePort` (compound), `setPortDirection`, `createPartUsage` (compound: N create-PortUsage + create-PartUsage + position; one Cmd-Z reverts the whole step), `setPartUsageMultiplicity`, `openInternalDiagram` (find-or-create IBD bound to a PartDefinition). (e) Registry `update<K>` guard now consults a `KIND_OPTIONAL_FIELDS` table so per-kind optional fields like `multiplicity` and `direction` aren't rejected as kind-mismatch on first assignment. (f) Visual baselines refreshed via `scripts/regen-baselines.sh` (Linux container) for `inspector-block-selected` and `project-tree-populated` (both now show the new Ports section / Parts group); three new baselines added (`ibd-one-part-no-ports`, `ibd-one-part-two-ports`, `ibd-two-parts`). Agent visual-inspection walk under `artifacts/iteration-21/` covers the 9-step interaction: empty BDD → block + ports → Open IBD → drop → picker → PartUsage with handles → inspector multiplicity → second part → undo (links: #50).
- 2026-05-12: Iteration 23 — PR #59 first CI run (25688946856) failed on `ibd-two-parts-one-connection` visual snapshot (chromium 9394 px diff, ratio 0.02 vs 0.01 threshold). Root cause: arm64-podman emulation on Apple Silicon produces font hinting that diverges from amd64 GitHub Actions runner; the divergence stays under 0.01 for BDD baselines but the IBD screen has more text (port labels + edge label) that pushes accumulated per-glyph diff over threshold. Earlier CONTEXT.md note "matches CI within tolerance" was over-confident. Fix: downloaded the failed CI's `playwright-report` via `gh run download`, identified the per-browser *actual* PNGs by file-size matching against the *expected*/diff PNGs (chromium 26fb32a5 56033B, webkit b398520a 52735B), copied them over the committed baselines. New baselines are bit-exact for the renderer that produces them next run, so the second CI cycle (25689283189) passed in 1.3 min, auto-merge fired, **PR #59 merged at e4229f6** and #51 closed. Updated `docs/CONTEXT.md` with the new preferred baseline-regen workflow (extract from failing CI rather than retry the arm64 container) (links: #51, PR #59).
- 2026-05-12: Iteration 22 — Shipped **#51** (ConnectionUsage edge with port-to-port wiring + typed compatibility). Decisions: (a) Extended `Viewpoint` with `acceptedEdgeElementKinds: readonly ElementKind[]` and `edgeTypeForElement(element)` so per-viewpoint *elements* that render as ReactFlow edges (ConnectionUsage today, ItemFlow next, Transition in state-machine) plug in without core changes — BDD's lists are empty. (b) `CanvasPane.toFlowEdges` now traverses elements once per recompute, using `buildPortUsageOwnership(elements)` (O(N+ports)) to resolve each element-edge's PortUsage handle to its parent PartUsage node id. (c) `isValidIbdConnection` enforces ADR-0003 typing (out↔in, inout↔anything; in↔in and out↔out rejected); `canonicalizeIbdConnection` flips `in→out` drags into canonical `out→in` so persisted ConnectionUsage `sourceId` always lives on the providing port. (d) Workspace action `connectPorts(connection)` runs the canonicaliser, dispatches `create-element` with cascading `connectionN` name, and returns the new id. (e) `IbdConnectionUsageEdge` custom edge component: thin solid Bezier path (BaseEdge), optional centred name label rendered via `<EdgeLabelRenderer>` with `pointerEvents:'none'` so it doesn't intercept clicks. (f) Inspector gains a `ConnectionUsageExtras` section showing read-only `producer.power (out)` / `consumer.intake (in)` endpoint labels resolved from the PortUsage → PartUsage chain. (g) **Two `onNodesChange`/`onEdgesChange` selection-handling fixes**: gate the selection-diff branch on `changes.some(c=>c.type==='select')` so non-select changes (dimensions/positions) don't clobber external `setSelection` calls; track `isConnectingRef` via `onConnectStart` / `onConnectEnd` (reset on a 100ms `setTimeout`, not `queueMicrotask` — RF emits the stray source-node `select:true` one render tick after `onConnectEnd`) so Backspace after a drag-create only deletes the new edge, not the source PartUsage. (h) Element-as-edge removal: `onEdgesChange remove` routes to `deleteElement` when the id resolves in the registry, falls through to `unlinkEdge` for ModelEdges. (i) Visual baseline `ibd-two-parts-one-connection.{chromium,webkit}.png` added; CONTEXT.md captures all four lessons (RF stray-select, edge `force:true` click bypass, Workspace metaKey||ctrlKey double-fire on `Meta+z+Control+z`, hasSelectChange gating) for future viewpoints (links: #51).
- 2026-05-12: Iteration 24 — Shipped **#52** (ItemFlow edge variant — directed flow + itemType label). Decisions: (a) Shift-modifier discriminates the two element-as-edge kinds in `CanvasPane.onConnect`: a `shiftHeldRef` (updated by a window-level `keydown`/`keyup` listener AND seeded from `onConnectStart`'s native-event `shiftKey`) routes the drop to `connectItemFlow` instead of `connectPorts` — same canonicaliser, same typing rules, different element kind. (b) `IbdItemFlowEdge` renders a dashed bezier (`strokeDasharray: '6 4'`) with a per-edge SVG `<marker>` triangle at the target end (`refX=11, refY=6, orient="auto-start-reverse"`, fill = stroke colour); ConnectionUsage stays solid + unmarkered so the two are visually unambiguous. (c) Label preference: `itemType` when set, else cascading default name `flow1`/`flow2`/… — picking up the name immediately so a fresh ItemFlow is selectable even before the user types an `itemType`. (d) Inspector's `ItemFlowExtras` section shows the same read-only endpoint labels as `ConnectionUsageExtras` plus an editable Item-type field; commits on blur or Enter via new `setItemFlowType` store action. (e) `connectItemFlow` mirrors `connectPorts` (dispatches `create-element` with the canonicalised endpoints) — one-line fork avoids duplicating the canonicalisation/validation path. (f) `onConnectStart`'s event arg is a native DOM `MouseEvent | TouchEvent` (NOT React's synthetic) — recorded as a CONTEXT.md gotcha because typing it as `ReactMouseEvent` fails the `OnConnectStart` signature. (g) Visual baseline `ibd-two-parts-one-itemflow.{chromium,webkit}.png` added via the arm64 podman container; left the freshly-restored `ibd-two-parts-one-connection.{...}` CI-extracted baselines from PR #59 untouched after the regen run rewrote them — if CI flags an arm64↔amd64 diff on the new ItemFlow baseline, the recovery path is the same CI-actual extraction trick (links: #52, PR #61).
- 2026-05-12: Iteration 25 — PR #61's first CI run (25690316163) failed on the webkit `ibd-two-parts-one-itemflow` baseline (9234 px diff, ratio 0.02 vs threshold 0.01) — exactly the arm64↔amd64 divergence the iteration-24 STATUS flagged as the contingency. Recovery path executed cleanly: extracted attachment name→sha1 map from `test.trace` inside the report's data zip (`actual sha1 = 53ba200f...`, 54759 B — *larger* than the 50572-byte expected), copied that PNG over the committed webkit baseline. Refined the CONTEXT.md recovery procedure to use the trace-metadata mapping rather than the size-based heuristic, because this run's actual was larger than the expected (anti-aliasing differences can swing compression either way). Second CI cycle (25690621584) green in 1.3 min, but main moved while CI ran (PR #62 STATUS update landed first), forcing a `gh pr update-branch --rebase` round-trip — same BEHIND state previously seen on PR #40 and #46. Third CI cycle (25690790606) green in 1.7 min, auto-merge fired, **PR #61 merged at 37269ae**. Phase 3 remaining: **#53** (cross-diagram navigation, Show in BDD/IBD context menu) and **#54** (Phase 3 gate spec, depends on #53) (links: #52, PR #61).
- 2026-05-12: Iteration 26 — Shipped **#53** (cross-diagram navigation — Show in BDD/IBD context menu). Decisions: (a) New `src/workspace/contextMenu/` directory with two files: pure `navTargets.ts` (deriveNavTargets returning ordered `NavTarget[]`) + presentational `ContextMenu.tsx` (role="menu" + role="menuitem", keyboard with Up/Down/Home/End/Enter/Space/Escape, focus restoration on close, outside-pointerdown dismissal). (b) Two new store actions plumb the model seams: `showDefinitionOnBdd(partUsageId)` switches to the first BDD and selects the typing PartDefinition; `navigateToElementOnDiagram(elementId, diagramId)` is the generic same-element form — both are direct `set()` updates (navigation is presentation state, not model state). (c) `CanvasPane` wires the menu via `onNodeContextMenu` / `onEdgeContextMenu` and only calls `event.preventDefault()` when `deriveNavTargets` returns ≥1 target, so the browser-native menu still shows on right-click of non-navigable elements (e.g. a BDD edge). (d) A11y axe scan caught `text-muted-foreground` on `bg-card` at 4.34:1 (vs WCAG AA 4.5:1) for normal-weight description text — fixed by swapping to `text-foreground/75` (opacity-based near-black). Recorded in CONTEXT.md for future popover/menu work. (e) Visual baseline `context-menu-bdd.{chromium,webkit}.png` committed via Linux container regen; click position adjusted to `(180, 70)` inside the block so the menu opens cleanly *below* the block boundary for a crisp baseline. (f) PR #64 CI run 25692098227 first green in 1.5 min, but main moved to 861a9ee during CI (status PR #63 landed), forcing the now-routine `gh pr update-branch --rebase` round-trip — second run 25692253937 green in 1.5 min, auto-merge fired, **PR #64 merged at de83399** (links: #53, PR #64).
- 2026-05-12: Iteration 27 — **Phase 3 closed, vphase-3 tagged at f14d6a1.** Phase 3 gate spec (#54) merged via PR #66 (CI run 25693460502 post-rebase, 142/142 chromium+webkit+visual+a11y in 1.5 min). One e2e orchestration covers the full IBD slice: create Engine+Cylinder PartDefinitions on BDD → add fuelIn/exhaustOut ports → right-click → Show in IBD → drag two Cylinder PartUsages → wire a ConnectionUsage + Shift-drag an ItemFlow (`itemType` "fuel") → reload to assert persistence → rename Engine via the project tree while focused on the IBD, jump back to BDD, verify "EngineV2" label reflects → Cmd-Z cascade across diagrams until ItemFlow → ConnectionUsage → PartUsages → ports → blocks have all rolled back in order. Two notable findings recorded in CONTEXT.md as iteration-27 entries (worth carrying forward): (a) Issue #54's literal step ordering ("drag PartUsages" before "add ports") is unsupported by the current model — `createPartUsage` materialises PortUsage children at PartUsage-creation time and adding a port to a PartDefinition does NOT propagate handles to existing PartUsages. The spec adds ports first to preserve every cross-diagram assertion. (b) After switching tabs in Playwright, React Flow rebuilds the edge layer one render tick AFTER the parts mount — querying the DOM during that gap returns zero edges even though the model has them. The cascade waits for `parts`/`connectionEdges`/`itemFlowEdges` to all reach the expected counts before the first Cmd-Z. Release workflow 25693655954 green; live deploy at https://michaeljfazio.github.io/mbse-workbench/ returns HTTP 200 with bundle hash `assets/index-BL_8FBhf.js` matching the build artifact. Four smoke screenshots (shell → 2 blocks → Show-in-IBD empty → 2 Cylinder PartUsages) uploaded as vphase-3 release assets. Phase 4 (Requirements Diagram) begins next iteration (links: #4 epic closed, #67 release closed, PR #66, vphase-3 tag).
- 2026-05-12: **Phase 4 decomposed into five child issues** (#70 viewpoint registration + ADR 0004, #71 Requirement custom node + palette drop + inspector RequirementExtras, #72 RequirementTrace edge variants with traceKind picker, #73 cross-diagram traceability via inspector "+ Link requirement", #74 Phase 4 gate spec). Reason: AGENT.md Ralph loop step 6 — just-in-time decomposition when a phase becomes current. Sequencing: #70 unblocks #71; #71 unblocks #72; #72 unblocks #73; #74 depends on all four. Linked from epic #5's task list. **Metamodel work is already done** — `RequirementElement` (with reqId/priority/status/text/rationale) and `RequirementTraceEdge` (with the four `RequirementTraceKind` values) shipped in Phase 1; Phase 4 is UI-layer only (links: #69).
- 2026-05-12: **ADR 0004 (Requirements Diagram shape)** to be authored on #70's PR (per the Phase 3 pattern where #49 introduced ADR 0003 inside the viewpoint-registration child rather than via the decomposition chore). Four key decisions deferred to that ADR: (a) multiple Requirements diagrams per project, no `Diagram.context` link (unlike IBD's PartDefinition coupling); (b) RequirementTrace stays in `ModelEdge` (NOT promoted to an element-as-edge like ConnectionUsage/ItemFlow) because it carries no own identity beyond `{sourceId, targetId, traceKind, label}`; (c) endpoint typing: `derive`/`refine` require both endpoints Requirement; `satisfy`/`verify` allow non-Requirement targets; (d) cross-diagram visibility: Phase 4 ships trace edges visible on the Requirements canvas only (inspector "Trace links" section handles the BDD/IBD seam); rendering trace edges directly on BDD/IBD is deferred to Phase 10's traceability matrix work (links: #69).
- 2026-05-12: Iteration 30 — Shipped **#71** (Requirement custom node + palette drop + inspector RequirementExtras). Decisions: (a) New `RequirementNode.tsx` is a 240×140 card with reqId tag, inline-renamable name, 3-line truncated text, and colour-coded `priority` + `status` badges built from Tailwind `bg-*-100`/`text-*-900` pairs (slate/sky/amber/rose for priority; slate/emerald/sky/violet/rose for status) so axe-AA passes on the card. Bold + uppercase + tracked text means even the small badges meet the "large/bold 3:1" rule rather than 4.5:1. (b) `requirementsViewpoint` registers the node at module scope, adds the `Requirement` palette item, and maps `Requirement → requirements-requirement`. (c) Workspace store `createRequirement(diagramId, position, options?)` wraps `create-element` + `update-diagram-position` in one compound command (single Cmd-Z reverts the whole step); cascading defaults are `Req1` / `Req2` / … and `R-001` / `R-002` / … (**scans from 1** so gaps from custom-reqId imports get filled rather than skipped); defaults `priority: 'medium'`, `status: 'draft'`, `text: ''`. (d) Per-field setters `setRequirementReqId / Text / Priority / Status / Rationale` dispatch typed `ElementPatch<'Requirement'>` with no-op + clear-on-empty semantics; `KIND_OPTIONAL_FIELDS.Requirement = ['reqId','rationale']` was already in place from the metamodel split. (e) `Inspector` adds `RequirementExtras` with the 5 fields — commits on blur, Cmd/Ctrl+Enter blurs the textarea, Escape rolls back drafts. (f) `CanvasPane` toolbar gains `+ Requirement` (parity with `+ Block`); the generic drop handler routes Requirement kinds to `createRequirement`. (g) `ProjectTree` shows a right-aligned monospace `font-mono text-[10px]` reqId subtitle on Requirement leaves; pinned to `text-foreground/75` (the muted shade failed at 3.88:1, same as the popover pattern from #53). (h) **Three new gotchas recorded in `docs/CONTEXT.md`**: Playwright `addInitScript` runs on every reload — gate seeds on existing sessionStorage; the regen-baselines script rewrites every baseline whose bytes diff from expected (revert unintended modifications before staging); arm64↔amd64 baseline divergence struck again on three new Requirements baselines + two pre-existing IBD ones — recovered via the iteration-25 CI-actual extraction procedure (`window.playwrightReportBase64` → JSON test→attachment map → copy by sha1). (i) PR #78 first CI run 25696285948 failed on 8 visual specs with the 0.02-ratio diff (5 new + 3 pre-existing). Second CI run 25696610065 (after baseline replacement) green in 1.9 min (168/168 — chromium + webkit + visual + a11y), auto-merge fired, **PR #78 merged at 495260e** (links: #71, PR #78).

- 2026-05-12: Iteration 29 — Shipped **#70** (Requirements viewpoint registration + ADR 0004). Decisions: (a) `requirementsViewpoint` registered alongside BDD/IBD with empty `nodeTypes`/`edgeTypes` (frozen module-scope) and empty `paletteItems`; `acceptedElementKinds: ['Requirement']`, `acceptedEdgeKinds: ['RequirementTrace']`, `acceptedEdgeElementKinds: []`. `nodeTypeFor`/`edgeTypeFor`/`edgeTypeForElement` throw with descriptive messages until #71/#72 land renderers. (b) Authored ADR 0004 covering scope (free-form, no `Diagram.context`), edge model (RequirementTrace in `ModelEdge`, not element-as-edge), endpoint typing (`derive`/`refine` Req→Req only; `satisfy`/`verify` Req→{Requirement, PartDefinition, PartUsage, ActionDefinition, ActionUsage, StateDefinition, StateUsage, UseCase}, self-loops rejected), and cross-diagram visibility (Phase 4 renders trace edges on Requirements canvas only; inspector "Trace links" panel handles BDD/IBD seam in #73). (c) Visual baselines `requirements-empty.{chromium,webkit}.png` regenerated via `scripts/regen-baselines.sh`; the arm64 container run also rewrote three earlier CI-extracted baselines (`ibd-two-parts-one-connection.{chromium,webkit}` and `ibd-two-parts-one-itemflow.webkit`) — reverted those back to their main-committed forms before staging, keeping only the two new baselines. (d) PR #76 first CI run 25694863185 green in 1.6 min (148/148: 7 new unit + 3 e2e × 2 browsers), auto-merge fired, **PR #76 merged at 361d7b7**. (e) Recorded the regen-script side effect in `docs/CONTEXT.md` (full-suite `--update-snapshots --grep @visual` will rewrite ANY baseline whose actual bit-diffs from expected, including the previously CI-extracted ones; revert unintended modifications before staging). Phase 4 remaining: **#71** (Requirement node + palette + inspector RequirementExtras), **#72** (RequirementTrace edge variants + traceKind picker), **#73** (cross-diagram traceability inspector seam), **#74** (Phase 4 gate spec, depends on the prior three) (links: #70, PR #76).

## Next action
Pick up **#72 (RequirementTrace edges — derive / satisfy / verify / refine — with traceKind picker)** next iteration. Pattern mirrors BDD's Composition/Generalization (#31), not IBD's element-as-edge: `RequirementTrace` is a `ModelEdge` discriminated-union variant (Phase 1 already shipped it). For each `traceKind` add a custom `<BaseEdge>` component under `src/viewpoints/requirements/` with kind-specific stroke style (solid for `derive`, dashed for `satisfy`, dotted for `verify`, double for `refine` — or use distinct hues; pick distinct visuals that survive the 4.5:1 contrast rule on the canvas background); register them in `requirementsViewpoint.edgeTypes` (frozen module-scope); fill out `edgeTypeFor(edge)` to switch on `edge.traceKind`. On `onConnect` in the Requirements viewpoint, mirror BDD's `EdgeKindPopover` — surface a 4-option picker (Derive / Satisfy / Verify / Refine) near the target node, then dispatch `link` with the chosen `traceKind`. ADR 0004 §3 typing rules: `derive` / `refine` require both endpoints Requirement; `satisfy` / `verify` allow non-Requirement targets in Phase 4 (Requirement→{Requirement, PartDefinition, PartUsage, ActionDefinition, ActionUsage, StateDefinition, StateUsage, UseCase}); self-loops rejected — wire into `isValidConnection` on the Requirements canvas. Inspector gains a `RequirementTraceExtras` section with read-only endpoint summary + editable `traceKind` select that fires `update-edge` (introduce the edge-update path if not present). Visual baselines for one-edge-per-kind plus the picker open. Cross-diagram visibility (rendering trace edges on BDD/IBD canvases) stays deferred to Phase 10 per ADR 0004 §4; Phase 4's seam for the BDD/IBD side is #73's inspector "+ Link requirement" section.
