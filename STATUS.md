# STATUS

## Current phase
phase:6 — State Machine Diagram (epic #7 open; child issues #104-#107 ready)

## Current iteration
- Iteration #: 44
- Started: 2026-05-12
- Branch: issue/105-state-usage-and-pseudostate-nodes
- Working on: **#105** — StateUsage + initial/final pseudostate custom nodes + palette chip strip + inspector StateExtras. Iteration 43's recovery push (60d033b) eventually merged via PR #108's second CI run (25707489536) at b3b9784 — the `bdd-two-blocks-linked-webkit.png` baseline was reported as flaky but Playwright retry recovered. Phase 6 progresses: #104 closed; #105 / #106 / #107 still open. This iteration scaffolds the entire #105 vertical: model gets `STATE_NODE_TYPE_VALUES` + `isStateNodeType`; new `StateUsageNode` component branches on `stateType` to render rounded-rectangle / filled-circle / bullseye-circle; `stateMachineViewpoint.nodeTypes` now wires the component for all three node-type strings; new `StateMachinePalette` chip strip mirrors `ActivityPalette` with two MIME types (`PROJECT_TREE_DRAG_TYPE='StateUsage'` + `PROJECT_TREE_DRAG_STATE_TYPE`); `CanvasPane` gains a `+ State` toolbar button, conditional palette render, and a State Machine `handleDrop` branch picking `stateType` from the second MIME; new store action `createStateUsage(diagramId, position, stateType, options?)` with the same compound-command pattern as `createActionUsage` plus three setters (`setStateEntryAction` / `setStateExitAction` / `setStateDoAction`); project tree leaf falls back to `«${element.stateType}»` for empty-named StateUsages (parallels the existing ActionUsage fallback); Inspector gains a `StateUsageExtras` panel showing the stateType badge and — for `'state'` only — three optional action fields (entry/do/exit) committed on blur/Enter. Unit tests: 12 new in `tests/unit/workspace/stateMachineActions.test.ts` + viewpoint test updated to assert the three populated `nodeTypes` entries. E2e: 12 new specs in `tests/e2e/state-machine-nodes.spec.ts` (toolbar+chips, drops, Cmd-Z, tree placeholders, reload-survives-action-fields, Cmd-Z-reverts-action-field, pseudostate-inspector-omits-fields, @a11y, three @visual baselines). Local `pnpm run check` fully green (typecheck + lint + 460 unit + build + 228 e2e on chromium + webkit). Visual baselines deferred to CI extraction per the iter-25 procedure: the four new baselines (`state-machine-with-state` + `state-machine-with-pseudostates` + `inspector-state-selected` × {chromium, webkit}) don't exist yet; CI will emit `*-actual.png` files for the iter-39 first-time pattern. The `state-machine-empty.{chromium,webkit}.png` baselines from #104 will likely also need refresh because the palette chip strip + `+ State` button now push canvas chrome (iter-37 lesson).

## Last test run
- Command: `pnpm test:e2e` + `pnpm test:unit` + `pnpm typecheck` + `pnpm lint` + `pnpm build`
- Result: PASS — 228 e2e passed (chromium + webkit), 460 unit tests passed, typecheck clean, lint shows only the two pre-existing `ActionUsageNode.tsx` react-refresh warnings, build green (588 modules, 544 kB).
- Failures: none. One e2e test required adjustment after the first local run: `Cmd-Z reverts an action-field edit` originally tabbed off the input which moved focus to the next inspector input — Cmd-Z then routed to the browser's native input-undo (per Workspace.tsx's INPUT-focus guard). Fix: press Enter on the entry input instead, which blurs to nothing per the StateActionField onKeyDown handler. Lesson worth recording: when testing model Cmd-Z after an inspector-field commit, prefer Enter-to-blur over Tab so focus leaves all inputs.

## Known issues / blockers
- (none)

## Decisions log
- 2026-05-11: Bootstrap as a single committed scaffold, not iterative through child PRs. Reason: AGENT.md Phase 0 explicitly lists scaffold steps as the bootstrap and instructs iteration 1 to "run Phase 0 bootstrap" when STATUS.md is missing; opening child issues against an empty repo with no CI yet would be the wrong order. Child issues for any *remaining* Phase 0 polish are opened after the initial commit.
- 2026-05-11: Pinned minor versions per AGENT.md "Version pinning" rule. React 18.3.x, Vite 5.4.x, Tailwind 3.4.x, @xyflow/react 12.3.x, Vitest 2.1.x, Playwright 1.48.x, Anthropic SDK 0.32.x, ESLint 8.57.x. Tailwind 3 (not 4) avoids the PostCSS landmine.
- 2026-05-11: `pnpm run check` composes typecheck + lint + unit + build + e2e in that order; the Playwright suite is the single source of truth for functional, visual (`@visual`-tagged) and accessibility (`@a11y`-tagged) specs across `chromium` and `webkit` projects.
- 2026-05-11: Repo name fixed to `mbse-workbench`; Pages base path `/mbse-workbench/` in production via `vite.config.ts`.
- 2026-05-11: Allow `vphase-*` and `v*.*.*` tags to deploy to the `github-pages` environment via environment deployment-branch-policies (type: tag). Reason: protection rule blocked the first vphase-0 deploy with a tag policy error; documented in `docs/CONTEXT.md` so future phases skip this.
- 2026-05-11: Phase 1 decomposed into 5 child issues (#17 metamodel, #18 registry, #19 command bus, #20 repository, #21 collaboration seams). Reason: AGENT.md Ralph loop step 6 — just-in-time decomposition. Linked from epic #2's task list.
- 2026-05-11: Metamodel split — 19 element kinds in `ModelElement` discriminated union; 9 pure structural relationships in `ModelEdge`. `ConnectionUsage` / `ItemFlow` / `Transition` are elements (named, selectable) with `sourceId`/`targetId` baked in; the diagram renderer derives edge geometry from either source. Recorded in [ADR 0002](docs/adr/0002-metamodel-shape.md) (links: #17).
- 2026-05-11: Registry `update<K>` replaces the stored element with a new object rather than mutating in place, so a `readonly ModelElement[]` snapshot taken before `update` is unaffected. Reason: command-bus undo/redo and future event-sourcing will rely on snapshot stability; in-place mutation would silently corrupt history. Runtime kind-check guards the case where the caller-chosen `K` does not match the stored element's `kind` (TS cannot infer K from a runtime id). Records dangling refs for the three element-kinds that carry `sourceId`/`targetId` directly (links: #18).
- 2026-05-11: Command bus shape — `Command` is a discriminated union of `create-element` / `update-element` / `delete-element` / `link` / `unlink` / `compound`. Generic over `ElementKind` and `EdgeKind` rather than 75 per-kind command types, because the metamodel is already a discriminated union and the registry validates kind via the existing `update<K>` runtime guard. ModelEvent payload = inverse command, so the append-only event log is self-undoable. Undo/redo stacks hold `{ forward, inverse, actor }` and just `applyOnly` (no re-capture). Delete-element's inverse is a `compound` of `create-element` + one `link` per incident edge captured at apply time (links: #19). **Extended 2026-05-12 by #72 with `update-edge` for edge-label round-trips.**
- 2026-05-11: Repository is a thin async port: one key per project at `mbse:v1:project:<id>`, ISO-string dates, `ProjectNotFoundError` covers both missing-key and malformed-JSON cases (caller treats them identically), `list()` silently skips malformed entries so one bad entry does not blind the picker, and `StorageQuotaError` wraps the underlying `QuotaExceededError` (and Firefox's `NS_ERROR_DOM_QUOTA_REACHED`). `Project` keeps `elements` / `edges` as readonly arrays so the consumer cannot mutate the cached value behind the registry's back (links: #20).
- 2026-05-11: Collab module split per responsibility (`user` / `presence` / `provider` / `permissions`) instead of one `types.ts` grab-bag. `User = { id, displayName, color }`; deterministic per-session color picked from `USER_COLORS` by hashing the id so the same id always renders identically across reloads. `can` (single-user default) returns true unless `target.ownerId` differs from `user.id`, giving Phase 1 a real ownership seam even though single-user mode never sets `ownerId`. Command bus accepts `provider?: CollaborationProvider` (defaults to Noop) and publishes every committed dispatch/undo/redo event post-apply — denied permissions throw *before* publish, so the provider only ever sees committed events (links: #21).
- 2026-05-11: Phase 1 closed and vphase-1 tagged at c1214db. Release workflow #25670869402 green; Pages HTTP 200; screenshot uploaded as `app-shell.png` release asset. Phase 1 shipped data layer only — deployed UI unchanged from vphase-0 (links: #27).
- 2026-05-11: Phase 2 decomposed into 7 child issues (#30 workspace shell + viewpoint registry, #31 BDD React Flow canvas + Block/Composition/Generalization, #32 inspector, #33 project tree + drag-from-tree, #34 dagre auto-layout + per-view positions, #35 PNG/SVG export, #36 Phase 2 gate e2e). Reason: AGENT.md Ralph loop step 6 — just-in-time decomposition. Sequencing: #30 unblocks #31; #31 unblocks #32-#35 (independent of each other); #36 depends on all. Linked from epic #3's task list (links: #29).
- 2026-05-11: `@xyflow/react` v12.3.x research findings recorded in `docs/CONTEXT.md` ahead of Phase 2 first use — Zustand wiring (drive via props + applyNodeChanges/applyEdgeChanges, route through command bus), required CSS import, `node.width`/`node.height` (not `style`), `nodrag`/`nopan` classes on interactive children, fresh `dagre.graphlib.Graph()` per layout call for strict-mode safety, built-in delete key. Reason: AGENT.md directive 11 — version pinning + library currency. Verified via context7-fetched authoritative v12 docs (links: #29).
- 2026-05-11: Workspace shell foundation (#30) committed at daa7c6f. Three-pane resizable layout (project tree / canvas / sidebar tabs), Zustand `useWorkspaceStore`, `Viewpoint<T>` registry with BDD stub, project bootstrap via `InMemorySessionRepository`, pane-width persistence to sessionStorage. Resize divider exposed as a keyboard-operable `role=separator` with `aria-valuenow`/`aria-controls`. PR #38 merged at 071eb3d.
- 2026-05-11: Visual snapshot baselines pinned to **Linux** renderer (CI runs ubuntu-latest). `playwright.config.ts` skips `@visual` specs when `!CI && platform !== 'linux'` so the agent's darwin `npm run check` doesn't flap on font hinting. Baselines live under `tests/e2e/__screenshots__/{testFileName}/{arg}-{projectName}.png` (via `snapshotPathTemplate`). Generation procedure: `mcr.microsoft.com/playwright:v1.48.2-jammy` via `podman --platform linux/arm64` + `vite preview` (Vite dev hits `ENOSPC` on file-watcher limits in the container). Full procedure recorded in `docs/CONTEXT.md` for future viewpoints (links: #30).
- 2026-05-11: `Viewpoint<T>` interface replaces placeholder `renderNode`/`renderEdge` with ReactFlow-native `nodeTypes` / `edgeTypes` (frozen module-scope) plus `nodeTypeFor(element)` / `edgeTypeFor(edge)` mappers. Reason: ReactFlow's typed custom-node/edge approach is the idiomatic seam; the placeholder methods were never used. Each viewpoint is now a single config registered via `viewpoints.register(...)` and `CanvasPane` consumes its `nodeTypes`/`edgeTypes` directly. Adding the next viewpoint (#33–#39 etc) requires zero core changes (links: #31).
- 2026-05-11: Per-view positions live on `Diagram` (`positions: Record<ElementId, {x,y}>`), not on the element. **Superseded 2026-05-11 by the #34 decision below** (positions are now first-class commands so manual drag + auto-layout both undo).
- 2026-05-11: Custom nodes must NEVER import `@/workspace/store` at module load — causes a load-time cycle. Callbacks like `onRename` pass through ReactFlow node `data`. Pattern applies to every viewpoint (links: #31).
- 2026-05-11: Edge markers (composition diamond, generalization triangle) are rendered as per-edge SVG `<marker>` defs inside each custom edge component, unique ids `bdd-{kind}-{shape}-${edgeId}`. ReactFlow's built-in `MarkerType` only covers open/closed arrows (links: #31).
- 2026-05-11: Block-node label is a non-interactive `<div onDoubleClick=...>` rather than a `<button>` — axe-core's `nested-interactive` rule otherwise fires. Inline rename still reachable via mouse double-click and via the inspector name field (links: #31).
- 2026-05-11: `vite.config.ts` honors `VITE_BASE_OVERRIDE` ahead of its `mode` check — required so the Linux baseline run-script's `vite preview` mounts at `/` (production CI builds keep the `/mbse-workbench/` base) (links: #31).
- 2026-05-11: Inspector (#32) ships the canvas → inspector → edit → reflect loop. New file `src/workspace/inspector/Inspector.tsx` renders empty / single / multi states; single-selection has `Name` + `Description` + read-only `Owner`. Workspace autosaves on every bus event so refresh persists. Registry `update<K>` guard whitelists optional ElementBase fields. Global Cmd/Ctrl+Z keyboard shortcuts route through the workspace store's undo/redo; suppressed inside text inputs (links: #32, PR #40).
- 2026-05-11: PR #40 needed a `gh pr update-branch --rebase` before auto-merge would fire — STATUS.md commit on main between PR open and CI completion put it in `mergeStateStatus: BEHIND` while branch protection has `strict: true`. Pattern recurs whenever STATUS.md lands during another PR's CI (links: #32, PR #40).
- 2026-05-11: **Position-as-command-bus** (#34) — `update-diagram-position` is now a first-class `Command` variant. Reverses the earlier #31 "presentation-only" decision because #34 requires single-step undo for both manual drag and auto-layout. `createBlock` wraps create-element + initial position in a compound. Mouse-up-without-move drags short-circuit so they don't pollute the undo stack (links: #34).
- 2026-05-11: Auto-layout (#34) is a single bus dispatch of a compound command containing N `update-diagram-position` sub-commands; one Cmd-Z reverts the whole layout (links: #34).
- 2026-05-11: Project tree (#33) — kind groups from union of (registry kinds, palette items across viewpoints); `draggable` only when kind is in the active viewpoint's palette. Drag MIME `application/x-mbse-element-kind`. Canvas drop validates against `acceptedElementKinds`, translates via `reactFlow.screenToFlowPosition`, auto-selects new element (links: #33, PR #42).
- 2026-05-11: Export (#35) — `src/workspace/export/` ships a pure `buildDiagramSvg` + thin PNG rasteriser at 2× DPR. `mbse-node` / `mbse-edge` class names are the contract for spec assertions. Toolbar `ExportMenu` split-button closes on outside-pointerdown / Escape (links: #35).
- 2026-05-11: Iteration 17 — AGENT.md's "refresh → Cmd-Z → link gone" sequence required persisting undo/redo stacks across page reload. Opened **#44** as a new Phase 2 child; `Project.history` now round-trips through the repository; CommandBus accepts `initialUndoStack` / `initialRedoStack` (links: #44, PR #45).
- 2026-05-12: **Phase 2 closed, vphase-2 tagged at 0f93af0.** Phase 2 gate (#36) PR #46 green (72/72). Release workflow 25681490005 green; Pages HTTP 200; smoke walkthrough captured. First release that the demo URL actually demos something. Phase 3 (IBD) begins next iteration (links: #3 epic closed, #47 release, PR #46).
- 2026-05-12: Phase 3 decomposed into six child issues (#49 IBD viewpoint registration + ADR 0003 IBD shape, #50 PartUsage node + PortUsage handles, #51 ConnectionUsage edge, #52 ItemFlow edge variant, #53 cross-diagram navigation, #54 Phase 3 gate spec). Linked from epic #4's task list (links: #48).
- 2026-05-12: `@xyflow/react` v12.3.x multi-handle-per-node research recorded — multiple `<Handle>` per side with unique `id` + inline `style={{ top: '<pct>%' }}`; `Connection.sourceHandle`/`targetHandle` carry the handle id; `isConnectable` and `isValidConnection` compose independently (links: #48).
- 2026-05-12: IBD viewpoint (#49) — `ibdViewpoint` registered alongside BDD. `Diagram.context?: { kind: 'partDefinition'; id }` added per ADR 0003 and round-trips through the repository. New workspace action `createDiagram(viewpointId, options?)`. Visual baseline `ibd-empty.{chromium,webkit}.png` committed (links: #49, PR #56).
- 2026-05-12: Axe `color-contrast` can sample tabs mid-CSS-transition and report phantom failures. Wait for `document.getAnimations()` `.finished` before `AxeBuilder.analyze()` in any `@a11y` spec that switches state. Playwright's `animations: 'disabled'` only applies to screenshot capture, not axe-core (links: #49, PR #56).
- 2026-05-12: Iteration 21 — Shipped **#50** (PartUsage node + PortUsage handles + port management). Added `PartUsageElement.portUsageIds: ElementId[]` so each PartUsage owns a stable list of PortUsage children mirroring `PartDefinition.portIds`. Drop on IBD opens a *"Type the part as…"* popover. Inspector gains `PartDefinitionExtras` (Ports list + Add port + Open Internal Diagram) and `PartUsageExtras` (read-only Definition + editable Multiplicity). Registry `update<K>` guard now consults a `KIND_OPTIONAL_FIELDS` table. Visual baselines refreshed (links: #50).
- 2026-05-12: Iteration 22 — Shipped **#51** (ConnectionUsage edge with port-to-port wiring + typed compatibility). Extended `Viewpoint` with `acceptedEdgeElementKinds` + `edgeTypeForElement` so per-viewpoint *elements* render as ReactFlow edges. `isValidIbdConnection` enforces ADR-0003 typing; `canonicalizeIbdConnection` flips `in→out` drags into canonical `out→in`. **Two onChange selection-handling fixes** baked in (RF stray-select after onConnect; hasSelectChange gating). Visual baseline `ibd-two-parts-one-connection.{chromium,webkit}.png` committed (links: #51).
- 2026-05-12: Iteration 23 — PR #59 first CI failed on `ibd-two-parts-one-connection` visual snapshot. **arm64-podman emulation on Apple Silicon produces font hinting that diverges from amd64 GitHub Actions runner** beyond `maxDiffPixelRatio: 0.01` on text-heavy screens. Preferred recovery: download CI's `playwright-report`, copy the `*-actual.png` files (via `test.trace` name→sha1 map) over the committed baselines. PR #59 merged at e4229f6 (links: #51, PR #59).
- 2026-05-12: Iteration 24 — Shipped **#52** (ItemFlow edge variant). Shift-modifier discriminates the two element-as-edge kinds in `CanvasPane.onConnect` (`shiftHeldRef` seeded by `onConnectStart`'s native-event `shiftKey` + window keydown/keyup listeners). `IbdItemFlowEdge` renders dashed bezier (`6 4`) + triangle marker at target. Inspector's `ItemFlowExtras` shows endpoint labels + editable Item-type. `onConnectStart`'s event arg is a native DOM `MouseEvent | TouchEvent`, not React's synthetic (links: #52, PR #61).
- 2026-05-12: Iteration 25 — PR #61's first CI failed on the webkit `ibd-two-parts-one-itemflow` baseline (9234 px diff). Recovery via the iteration-23 procedure; refined CONTEXT.md note (use trace-metadata mapping rather than file-size heuristic — anti-aliasing differences can swing compression either way). PR #61 merged at 37269ae (links: #52, PR #61).
- 2026-05-12: Iteration 26 — Shipped **#53** (cross-diagram navigation — Show in BDD/IBD context menu). New `src/workspace/contextMenu/` with pure `navTargets.ts` + presentational `ContextMenu.tsx`. Two new store actions `showDefinitionOnBdd` / `navigateToElementOnDiagram` — direct `set()` updates (navigation is presentation state). A11y axe caught `text-muted-foreground` on `bg-card` at 4.34:1 — fixed by switching to `text-foreground/75` for normal-weight description text. PR #64 merged at de83399 (links: #53, PR #64).
- 2026-05-12: Iteration 27 — **Phase 3 closed, vphase-3 tagged at f14d6a1.** Phase 3 gate (#54) PR #66 green (142/142). One e2e covers create Engine+Cylinder → ports → Show in IBD → drag PartUsages → wire ConnectionUsage + ItemFlow → reload → rename across diagrams → Cmd-Z cascade. Two recorded findings: `addPortToDefinition` does NOT cascade to existing PartUsages (so #54 spec adds ports BEFORE PartUsages); after tab-switch React Flow rebuilds the edge layer one render tick AFTER the parts mount (wait for both counts before count-driven loops). Release workflow green; smoke screenshots uploaded (links: #4 epic closed, #67 release closed, PR #66, vphase-3 tag).
- 2026-05-12: **Phase 4 decomposed into five child issues** (#70 viewpoint registration + ADR 0004, #71 Requirement custom node + palette drop + inspector RequirementExtras, #72 RequirementTrace edge variants with traceKind picker, #73 cross-diagram traceability via inspector "+ Link requirement", #74 Phase 4 gate spec). Metamodel work was already done in Phase 1 — Phase 4 is UI-layer only (links: #69).
- 2026-05-12: **ADR 0004** authored on #70's PR. Four key decisions: free-form Requirements diagrams (no `Diagram.context`); RequirementTrace stays in `ModelEdge` (not element-as-edge); endpoint typing per kind (`derive`/`refine` Req→Req only; `satisfy`/`verify` allow non-Requirement targets); Phase 4 renders trace edges on the Requirements canvas only (cross-diagram visibility is the #73 inspector seam) (links: #69, #70).
- 2026-05-12: Iteration 29 — Shipped **#70** (Requirements viewpoint registration + ADR 0004). `requirementsViewpoint` registered alongside BDD/IBD with empty `nodeTypes`/`edgeTypes` (frozen module-scope). PR #76 first CI run 25694863185 green; auto-merge fired, **PR #76 merged at 361d7b7**. The `scripts/regen-baselines.sh` full-suite `--update-snapshots --grep @visual` will rewrite ANY baseline whose actual bit-diffs from expected; revert unintended modifications before staging (links: #70, PR #76).
- 2026-05-12: Iteration 30 — Shipped **#71** (Requirement custom node + palette drop + inspector RequirementExtras). `RequirementNode.tsx` is a 240×140 card with reqId tag, inline-renamable name, 3-line truncated text, and colour-coded `priority` + `status` badges. `createRequirement(diagramId, position, options?)` wraps `create-element` + `update-diagram-position` in one compound command. reqId scans `R-001`, `R-002`, … **from 1 upwards** so gaps fill. Per-field setters dispatch typed `ElementPatch<'Requirement'>`. Project tree shows a monospace `font-mono text-[10px]` reqId subtitle. Playwright `addInitScript` runs on every reload — gate seeds on existing sessionStorage. PR #78 merged at 495260e (links: #71, PR #78).
- 2026-05-12: Iteration 33 — Shipped **#74** (Phase 4 gate spec). One orchestration `tests/e2e/phase-4-gate.spec.ts` walks the full Phase 4 vertical slice: drop two Requirements via the project tree → edit R-001's reqId/name/priority/status/text/rationale via inspector → reload (asserts every field persisted via sessionStorage) → drag a derive trace R-002→R-001 → switch to BDD → create Engine block → satisfy with R-001 + verify with R-002 via the inspector "+ Link requirement" popover → asserts model edges have correct sourceId/targetId/traceKind per ADR 0004 §3 → Cmd-Z cascade until model is empty → Cmd-Shift-Z cascade forward to 3 elements + 3 edges → reload → assert post-redo state survives reload. Plus three @a11y scans (Requirements empty, Requirements with two reqs + one derive trace, BDD block with one trace link). **Selection gotcha caught:** `CanvasPane.onNodesChange` MERGES new node-selects with the workspace-global `selectedElementIds` (preserving cross-diagram selections), so clicking a BDD block right after a Requirement/trace edge was selected leaves the inspector in 2-element state and `inspector-name` never appears — the gate's `addBlockNamed` now selects via `project-tree-leaf-${id}` (which REPLACES selection) instead of the canvas node click. Recorded in `docs/CONTEXT.md`. **Unrelated baseline drift caught:** the first CI failed on `bdd-two-blocks-linked-chromium.png` at ratio 0.02 — the baseline pre-dated #82's TraceLinksExtras addition (the spec selects Block 2 at shot time, so the right pane now renders the new "Linked requirements" section). Refreshed via the iter-23/25 trace-metadata procedure. PR #84 merged at 4b69312 after a `gh pr update-branch` (BEHIND main after PR #83 landed). (links: #74, PR #84).
- 2026-05-12: **Phase 4 closed, vphase-4 tagged at 4b69312.** All five Phase 4 children merged. Release workflow 25701434686 green; Pages HTTP 200 at https://michaeljfazio.github.io/mbse-workbench/; smoke walkthrough exercised the live deploy with seeded R-001 (Mission, critical/approved) + R-002 (Subfunction, high/draft) + derive trace and a fresh Engine PartDefinition with a satisfy trace to R-001 — screenshots in `artifacts/release-vphase-4/`. Phase 5 (Activity Diagram) decomposition starts next iteration (links: #5 epic closed, #85 release, PR #84, vphase-4 tag).

- 2026-05-12: Iteration 32 — Shipped **#73** (cross-diagram traceability — inspector "+ Link requirement" + show-traces context menu). Decisions: (a) `TRACE_TARGET_KINDS: ReadonlySet<ElementKind>` + `isTraceTargetKind(kind)` helper renamed from the private `SATISFY_VERIFY_TARGET_KINDS` set and re-exported from `@/viewpoints`; same eight kinds (Requirement / PartDefinition / PartUsage / ActionDefinition / ActionUsage / StateDefinition / StateUsage / UseCase) drive (i) the Inspector's `TraceLinksExtras` rendering, (ii) the context-menu "Show requirement traces" filter. (b) New Inspector section `TraceLinksExtras` shows every incoming RequirementTraceEdge as a row with `«kind»` stereotype + source Requirement name + reqId + unlink trash. "+ Link requirement" button opens `LinkRequirementPopover` — fixed-position so it escapes the inspector pane's overflow. (c) `LinkRequirementPopover` is a search input + radio list of Requirements + four kind buttons; satisfy/verify enabled for non-Requirement targets, all four for Requirement targets per ADR 0004 § 3. Single component, no two-step flow — click a kind to confirm + close. (d) `deriveNavTargets` extended with an `edges: readonly ModelEdge[]` field and `actions.showRequirementTracesFor`; emits `show-requirement-traces` when the element is a trace target with ≥1 incoming trace AND a Requirements diagram exists. Description pluralises ("1 trace" vs "N traces on <diagram name>"). (e) New store action `showRequirementTracesFor(elementId): DiagramId | null` mirrors `showDefinitionOnBdd`: switches active diagram to the first Requirements diagram + selects the source Requirement of the first matching trace. Returns null when no Requirements diagram or zero incoming traces. (f) Registry cascade already symmetric (`sourceId === id OR targetId === id`); new `registry.cascadeTrace.test.ts` documents target-side behaviour explicitly for #73's acceptance criteria. (g) **Two new CONTEXT.md gotchas**: `<ul role="radiogroup">` + `<li>` children fails axe `listitem` (the role override kills the implicit list parent role) — use `<div role="radiogroup">` with `<button role="radio">` siblings instead. Playwright's `toContainText` reads `textContent` which does NOT include `<input value="...">` — use `toHaveValue` instead. (h) Per ADR 0004 § 4, RequirementTrace edges still render on the Requirements canvas only; rendering on BDD/IBD is deferred to Phase 10 (traceability matrix). (i) PR #82 first CI run failed on all four new visual baselines (same arm64↔amd64 divergence as iter-23/25/31); applied the iter-25 recovery procedure (download `playwright-report`, parse `test.trace` for the name→sha1 map, copy `*-actual.png` over the baselines). Cross-referenced sha1s against the committed baselines to disambiguate chromium vs webkit since `browserName` is empty in the v1.48.2 trace metadata. Second CI run passed bit-exact. **PR #82 merged at 75e525b** (links: #73, PR #82).

- 2026-05-12: **Phase 5 decomposed into four child issues** (#87 viewpoint registration + ADR 0005, #88 Action + pseudostate custom nodes + palette drop + inspector ActionExtras, #89 ControlFlow + ObjectFlow edges with shift-modifier discrimination, #90 Phase 5 gate spec). Sequencing: #87 unblocks #88; #88 unblocks #89; #90 depends on all three. Metamodel work was already done in Phase 1 — `ActionUsage` already discriminates pseudostates via `nodeType: ActionNodeType` (`'action' | 'initial' | 'final' | 'fork' | 'join' | 'decision' | 'merge'`); `ControlFlowEdge` and `ObjectFlowEdge` already exist in `ModelEdge`. Phase 5 is UI-layer only, like Phase 4 (links: #6, #87, #88, #89, #90).

- 2026-05-12: Iteration 35 — Shipped **#87** (Activity viewpoint registration + ADR 0005). Decisions: (a) `activityViewpoint` registered alongside BDD / IBD / Requirements in `buildViewpointSingleton`. Frozen module-scope empty `nodeTypes` + `edgeTypes` records (populated by #88 / #89). Seven `ActionUsage` palette items with `defaultData.nodeType` covering all `ActionNodeType` discriminators. (b) `nodeTypeFor(element)` maps `ActionUsage.nodeType` to one of seven module-scoped node-type strings (`ACTIVITY_ACTION_NODE_TYPE`, `ACTIVITY_INITIAL_NODE_TYPE`, ...). `edgeTypeFor(edge)` maps `ControlFlow` + `ObjectFlow` to the two future edge-type strings. (c) `acceptedElementKinds: ['ActionUsage', 'ActionDefinition']` — Phase 5 renders only ActionUsage but reserving ActionDefinition keeps the door open for a future "called activity" frame without an ADR revision. (d) `acceptedEdgeKinds: ['ControlFlow', 'ObjectFlow']`, `acceptedEdgeElementKinds: []` (ControlFlow/ObjectFlow stay in `ModelEdge`, not element-as-edge — see ADR 0005 § 3). (e) `PaletteItem` extended with optional `defaultData?: Readonly<Record<string, unknown>>` so each of the seven Activity palette entries carries its `nodeType` discriminator through to element creation in #88. (f) **ADR 0005** authored covering five shape decisions: free-form scope (no `Diagram.context`), pseudostates as `nodeType` tags on ActionUsage, ControlFlow + ObjectFlow as `ModelEdge` entries, endpoint typing (`initial` cannot be target / `final` cannot be source / no self-loops), shift-modifier `onConnect` discrimination mirroring IBD #52. (g) Three new e2e specs in `tests/e2e/activity-empty.spec.ts` (Activity tab + canvas mounts; `@a11y` axe scan; `@visual` baseline). Visual baselines `activity-empty.{chromium,webkit}.png` generated via the Linux container; reverted nine previously-committed baseline overwrites per `docs/CONTEXT.md`'s 2026-05-12 regen-script procedure. (h) PR #92's first CI hit the same `bdd-two-blocks-linked-chromium` 0.02-ratio flake recorded in iteration-33 — Playwright's built-in retry recovered, so no baseline refresh was needed this time. PR #91 (iter-34 STATUS update) landed during my CI, leaving #92 BEHIND main; `gh pr update-branch --rebase` re-ran CI clean. **PR #92 merged at 674ef1e** (links: #87, PR #92).

- 2026-05-12: Iteration 36 — Mid-PR recovery on **#88** (Action + pseudostate custom nodes + palette drop + inspector ActionExtras). PR #94's first CI (run 25704052607) failed on all six new visual baselines (chromium + webkit × `activity-with-action` / `activity-with-pseudostates` / `inspector-action-selected`) at ratios ~0.02 — the now-familiar iter-23/25/31/32 arm64↔amd64 hinting divergence. Applied the documented recovery: `gh run download --name playwright-report`, unzipped each `data/*.zip`, parsed `test.trace` for the per-attachment expected→actual sha1 pairing (the `expected` sha1 cross-references the committed baseline to disambiguate chromium vs webkit since v1.48.2 traces don't carry `browserName`), copied the six `data/*.png` actuals over the baselines, committed at bad6334 and pushed. Branch had also been auto-rebased by `gh pr update-branch` between the original push and my fetch (origin moved `b401a1d → 8c287d1` after iter-35 STATUS landed on main during #94's CI), so `git rebase origin/issue/88-...` cleanly applied the refresh on top. New CI run 25704285916 in progress; auto-merge still enabled (links: #88, PR #94).
- 2026-05-12: Iteration 38 — Opened **PR #97** for #89 (ControlFlow + ObjectFlow edges). Decisions: (a) Validator `isValidActivityConnection` enforces ADR 0005 § 4 — both endpoints must be ActionUsage, no self-loops, `initial` cannot be a target, `final` cannot be a source — and the Activity branch of `<ReactFlow isValidConnection>` dispatches to it so invalid drops are refused inline. (b) Edge kinds stay in `ModelEdge` (not element-as-edge) per ADR 0005 § 3, so the two new components consume `EdgeProps<MyEdge>` and read endpoint + per-kind optional from `data.guard` / `data.itemType`. ControlFlow is solid bezier + filled triangle (`refX=11/refY=6`, `orient="auto-start-reverse"`); ObjectFlow is dashed `6 4` + hollow triangle filled with `--background`. Both add the SVG-side `g[data-edge-kind="ControlFlow"|"ObjectFlow"]` discriminator per the iter-31 CONTEXT.md lesson — `EdgeLabelRenderer` portals labels out of the SVG, so `[data-testid^=...]` matches the `<g>` AND each portaled label div; use the SVG-side `data-edge-kind` for counts. (c) Four new store actions wrap the existing command bus: `connectControlFlow` / `connectObjectFlow` dispatch `link` with the picked edge kind; `setControlFlowGuard` / `setObjectFlowItemType` dispatch `update-edge` via the registry's per-kind optional whitelist (`KIND_OPTIONAL_FIELDS.ControlFlow={guard}`, `KIND_OPTIONAL_FIELDS.ObjectFlow={itemType}` were already in place from the Phase 1 metamodel split). (d) Inspector adds dedicated `InspectorControlFlowEdge` + `InspectorObjectFlowEdge` panels, mirroring `InspectorTraceEdge` from #72: read-only Source/Target rendered via a new `describeActivityEndpoint` helper that falls back to `«${nodeType}»` for unnamed pseudostates, plus editable Guard or Item-type field. The top-level Inspector dispatch branches at the same place that already handles RequirementTrace. (e) `CanvasPane.onConnect` adds an Activity branch that picks `connectObjectFlow` vs `connectControlFlow` from the existing `shiftHeldRef` (initialised by `onConnectStart`'s native-event `shiftKey` + window keydown/keyup listeners — same plumbing as IBD #52). (f) `toFlowEdges` propagates `guard` / `itemType` / `label` into the per-edge `data` payload so the EdgeProps render the right label. (g) 21 unit tests for the validator (all eight kind pairs × initial-as-target / final-as-source rejections + self-loop + ghost ids + non-ActionUsage + null source/target). 8 Playwright functional + a11y specs in `tests/e2e/activity-edges.spec.ts`: drag → ControlFlow inspector reflects; Shift+drag → ObjectFlow; final-as-source rejected; initial-as-target rejected; guard edit → bracketed label → Cmd-Z reverts; itemType edit → label on edge; Backspace + Cmd-Z restore; axe scan with one ControlFlow zero serious/critical. (h) Two new @visual baselines pending: `activity-with-control-flow.{chromium,webkit}.png`, `activity-with-object-flow.{chromium,webkit}.png` — first CI may need the iter-25 playwright-report extract per the recorded arm64↔amd64 hinting divergence; the existing `activity-empty` / `activity-with-action` / `activity-with-pseudostates` / `inspector-action-selected` baselines should NOT need refresh because #89 adds no new persistent canvas chrome (the palette already exists from #88, no toolbar buttons added). Local `pnpm run check` green (440 unit + 198 e2e on chromium + webkit; visuals skipped on darwin). Rebased onto origin/main 708cce8 (PR #96 iter-37 STATUS landed during work). Auto-merge enabled; CI run 25705351434 queued at 2d2357a (links: #89, PR #97).

- 2026-05-12: Iteration 37 — **Shipped #88**. The second CI run on PR #94 (25704285916) still failed — but on the *pre-existing* `activity-empty.spec.ts` baseline from iteration 35 (#87), not the six new #88 baselines. #88 adds the `ActivityPalette` chip strip below the canvas toolbar (only visible on the Activity viewpoint); the strip pushes ~25 px of new content into the visible region, putting 9394 px (chromium) / 9613 px (webkit) of diff against the pre-#88 baseline — above the 0.01 threshold. Diagnosed from the failed run's diff PNG (red overlay across the new palette region plus the usual whole-page font-hinting variance), refreshed the two baselines via the iter-25 procedure (matched expected sha1s ec037af9 = chromium / 409c3ccd = webkit), pushed 1c1f39f. CI 25704499853 green; auto-merge picked it up but main had moved (PR #95 with iteration-36 STATUS landed at a8a4d37), so #94 went BEHIND. `gh pr update-branch --rebase` rebased to d3582a0, CI 25704704774 green, **PR #94 merged at e31bd02**. New `docs/CONTEXT.md` lesson: when a viewpoint phase adds persistent canvas chrome (palette / toolbar overlay / header band), the earlier-committed `*-empty` baseline on the same canvas becomes stale too — not just the new spec's baselines. Plan the regen step to include sibling baselines on the affected canvas (links: #88, PR #94, e31bd02).
- 2026-05-12: Iteration 39 — **Shipped #89** (ControlFlow + ObjectFlow edges). PR #97's first CI run 25705351434 failed with four `Error: A snapshot doesn't exist at .../activity-edges.spec.ts/activity-with-{control,object}-flow-{chromium,webkit}.png, writing actual.` — the previous iteration shipped the spec file + the implementation code but never created the `tests/e2e/__screenshots__/activity-edges.spec.ts/` baseline directory at all, so Playwright wrote actuals into `test-results/` and the spec failed. Pivoted from the established iter-25 mid-PR-baseline-refresh procedure (which assumes the baseline EXISTS and is bit-stale) to a direct first-commit path: parsed the playwright-report's per-spec JSON (embedded as a base64 zip inside `index.html`, decoded via `python3 + zipfile`) to extract the four actual PNG paths from the per-test `attachments[].path` entries (sha1 lookup via `name: "*-actual.png"`), copied them under the new baseline directory, spot-checked the chromium PNGs visually (control flow = solid bezier + filled triangle; object flow = dashed bezier + Token label + hollow triangle). Pushed 024212b; CI run 25705627780 went green on the second run. Main had moved during CI (PR #98 with iter-38 STATUS landed at 19a27fe), so PR #97 went BEHIND; `gh pr update-branch --rebase` to baefb2c, CI 25705811745 green, **PR #97 merged at cebfe67**. **New lesson worth adding to `docs/CONTEXT.md`:** when a phase ships a brand-new spec file with @visual specs, the implementing PR must also create the parallel `tests/e2e/__screenshots__/<spec-file>.spec.ts/` directory and populate it with the first-time baselines — there is no "stale baseline" to refresh from, so the iter-25 sha1-comparison cross-check doesn't apply. The path is simpler (just copy the actuals over) but requires extracting from the report's embedded zip rather than from a top-level `data/<sha1>.zip` (Playwright v1.48.2 ships the per-test report JSON inside `index.html`'s base64 blob — extract via `re.findall(r'[A-Za-z0-9+/]{1000,}={0,2}', html)[1]` + `base64.b64decode` + `zipfile.ZipFile`). After #97 merged, Phase 5 has only #90 (gate spec) remaining to close the epic (links: #89, PR #97, cebfe67).
- 2026-05-12: Iteration 31 — Shipped **#72** (RequirementTrace edges — derive/satisfy/verify/refine + traceKind picker). Decisions: (a) Extended the command bus with a first-class `update-edge` command (mirrors `update-element`) so the inspector's edge-label edit gets single-step undo. Added `registry.updateEdge<K>` with `EdgePatch<K>` (forbids id/kind/sourceId/targetId; allows `label` + per-kind optionals like `ControlFlow.guard`). Bus inverse-captures `previousValues` by key. (b) Single `RequirementTraceEdge` component branched on `data.traceKind`: dashed (`6 4`) for `derive`/`refine`, solid for `satisfy`/`verify`; per-edge SVG `<marker>` triangle at target (`req-trace-${edgeId}` id, hollow fill = `--background`); stereotype label `«kind»` rendered via `<EdgeLabelRenderer>` with optional user `label` line below. Pinned to `text-foreground/80` for axe-AA contrast. (c) `isValidRequirementTraceConnection(connection, registry, traceKind)` enforces ADR 0004 § 3; `validTraceKindsFor(...)` returns the subset valid for a given endpoint pair and drives the picker's disabled state. (d) New `TraceKindPopover` (pattern from `EdgeKindPopover`): role=dialog, Escape cancels, Enter confirms default, outside-pointerdown dismissal, disabled buttons for invalid kinds. (e) `CanvasPane.onConnect` branches on `viewpoint.id === REQUIREMENTS_VIEWPOINT_ID`: opens the picker near the target node; `confirmPendingTrace` dispatches `link` with the chosen `traceKind` and auto-selects the new edge. (f) `toFlowEdges` propagates `traceKind` + `label` into the flow-edge `data` payload. (g) `onEdgesChange` now selects ModelEdges (alongside element-as-edge ids) so RequirementTrace surfaces in the inspector after click — gated by `hasSelectChange` like the IBD path. (h) New `InspectorTraceEdge` panel shows read-only Source/Target/Kind rows + editable Label input. The Inspector branches at the top between element vs edge lookup. (i) Added `<Handle>` source/target to `RequirementNode` — forgotten in #71 because nothing connected to Requirements yet; Playwright drag tests would otherwise have no handle to grab. (j) Three new visual baselines committed via Linux container: `requirements-four-traces.{chromium,webkit}.png`, `requirements-trace-kind-popover.{chromium,webkit}.png`, `inspector-trace-edge-selected.{chromium,webkit}.png`. Reverted 10 unintended rewrites to previously-committed baselines per the iteration-30 lesson. (k) **Three new CONTEXT.md gotchas**: `update-edge` command shape; `<Inspector />` element-vs-edge dispatch via top-level lookup branch; `EdgeLabelRenderer` portals OUT of the canvas SVG so `[data-testid^=...]` matches the `<g>` AND the label divs — use a SVG-side discriminator like `g[data-trace-kind]` for counts. (l) PR #80 first CI run failed on all six new visual baselines (13599 px diff on chromium-four-traces — same arm64↔amd64 divergence as iter-23/25); followed the iteration-25 recovery procedure (download `playwright-report`, parse `test.trace` for the name→sha1 map, copy `*-actual.png` over baselines) and the second CI run passed bit-exact. **PR #80 merged at 6469f34** (links: #72, PR #80).

- 2026-05-12: Iteration 40 — Opened **PR #100** for #90 (Phase 5 gate spec). One orchestration `tests/e2e/phase-5-gate.spec.ts` walks the full Phase 5 vertical slice: drop seven palette chips (initial + Validate + Adult? + Approve + Reject + Done + final), rename five via the inspector, wire seven ControlFlows (Initial→Validate→Adult? → branches into Approve/Reject → both join Done → Done→Final), set Guard `age >= 18` on the auto-selected Adult?→Approve edge, shift-drag a second Approve→Done for an ObjectFlow with itemType `Token`, reload + assert sessionStorage round-trip, Cmd-Z cascade to empty, Cmd-Shift-Z cascade until 7 elements + 8 edges + guard + itemType all restored, final reload + assert. Plus three @a11y scans. **Two notable lessons baked into the spec:** (a) redo termination on 4 signals, not 2 — the trailing two `update-edge` commands (guard + itemType) don't change element/edge counts, so a count-only redo loop exits early; the fix is to also check `hasGuard` and `hasItemType` in the termination condition. (b) Canvas drop target is ~540 px tall on the 1280×800 viewport with default pane widths; first-draft layouts that put nodes at y > 540 fail Playwright's actionability check. No new @visual baselines per #90's "optionally" clause. Auto-merge enabled (links: #90, PR #100).

- 2026-05-12: Iteration 41 — **Phase 5 closed, vphase-5 tagged at ea62765.** PR #100 CI run 25706426965 green (264 e2e on chromium + webkit, 4m31s); auto-merge fired and merged at ea62765. Closed epic #6, opened release issue #102, tagged `vphase-5` on main. Release workflow 25706624668 green (build 19s → deploy 8s → github-release 5s). Pages HTTP 200 at https://michaeljfazio.github.io/mbse-workbench/. Smoke walkthrough script `scripts/smoke-vphase-5.mjs` seeded a 7-node approval workflow (initial + Validate + Adult? + Approve + Reject + Done + final) with 7 ControlFlows + 1 ObjectFlow + `age >= 18` guard + `Token` itemType, captured 4 PNGs under `artifacts/release-vphase-5/` (shell, full activity graph, ControlFlow inspector with guard, ObjectFlow inspector with itemType). Live deploy demonstrates behavioral modeling for the first time. JOURNAL `phase-completion` entry appended (links: #6 epic closed, #102 release, PR #100, vphase-5 tag).

- 2026-05-12: Iteration 42 — Phase 6 decomposed into four child issues earlier this session (#104 viewpoint registration + ADR 0006, #105 StateUsage + initial/final pseudostate nodes + palette + inspector StateExtras, #106 Transition element-as-edge + isValidStateMachineConnection + inspector TransitionExtras, #107 Phase 6 gate spec). Sequencing: #104 unblocks #105; #105 unblocks #106; #107 depends on all three. Metamodel work was already done in Phase 1 — `StateUsage.stateType: 'state' | 'initial' | 'final'`, `Transition` element-as-edge with `sourceId`/`targetId` + optional `trigger`/`guard`/`effect`. Phase 6 is UI-layer only, like Phases 4 and 5.

- 2026-05-12: **#104 closed, PR #108 merged at b3b9784.** Iteration 43's `inspector-block-selected-webkit.png` baseline refresh (60d033b) reset CI; second run 25707489536 cleared with a flaky-recovered `bdd-two-blocks-linked-webkit` retry. State Machine viewpoint registration + ADR 0006 now on main. Phase 6 progresses to #105 (links: #104, PR #108, b3b9784).

- 2026-05-12: Iteration 44 — Shipped #105 implementation (StateUsage + pseudostate custom nodes + palette + inspector StateExtras). Decisions: (a) Added `STATE_NODE_TYPE_VALUES` + `isStateNodeType` to model so the palette chip-strip and `CanvasPane.handleDrop` can mirror the Activity pattern with a typed runtime array. (b) **One `StateUsageNode` component for all three stateType strings**, branching internally on `data.stateType` (parallels `ActionUsageNode`). Action lines render below the name only when entry/do/exit are set — keeps the visual quiet for empty states. (c) **New MIME type `PROJECT_TREE_DRAG_STATE_TYPE` = `application/x-mbse-state-node-type`** mirrors `PROJECT_TREE_DRAG_NODE_TYPE`. Two-MIME drag is the established cross-viewpoint pattern (CONTEXT.md "Activity palette UI is a horizontal chip strip..."). (d) `createStateUsage` is a compound command — `create-element` + initial `update-diagram-position` — so a single Cmd-Z reverts the whole drop. Initial / final pseudostates skip the cascading default name; project tree leaf renderer falls back to `«${stateType}»` for empty-named StateUsages (same fallback already existed for ActionUsage; extended the conditional rather than duplicating). (e) Inspector `StateUsageExtras` renders the stateType badge for any StateUsage but only shows the three optional action fields for `stateType === 'state'` (initial/final cannot carry actions per the issue's "omits the entry/exit/do fields" criterion). A shared `StateActionField` component handles the three fields, dispatching to the appropriate setter via a `field` discriminator. (f) **e2e Cmd-Z-after-inspector-edit lesson**: pressing Tab after `fill()` moves focus to the next inspector input, which is *still* an INPUT — so Workspace.tsx's keyboard-shortcut guard suppresses Ctrl+Z (route to native input-undo) instead of model undo. Press Enter on the field's `onKeyDown` instead — it blurs to nothing, allowing the global undo handler to fire. Added to CONTEXT.md draft. (g) Visual baselines deferred to CI: six new files + a likely refresh of `state-machine-empty.*` (palette chip strip + `+ State` button = new canvas chrome — iter-37 viewpoint-canvas-chrome lesson). The iter-39 first-time-baseline extraction procedure handles the new files; iter-25 procedure handles the empty refresh if needed. Local `pnpm run check` fully green (links: #105).

- 2026-05-12: Iteration 42 — Opened **PR for #104** (Register State Machine viewpoint + ADR 0006). Decisions: (a) `stateMachineViewpoint` registered alongside BDD/IBD/Requirements/Activity in `buildViewpointSingleton`. Frozen module-scope empty `nodeTypes` + `edgeTypes` records (populated by #105 / #106). Three `StateUsage` palette items with `defaultData.stateType` covering all `StateNodeType` discriminators (`state` / `initial` / `final`). (b) `nodeTypeFor(element)` maps `StateUsage.stateType` to one of three module-scoped node-type strings; `edgeTypeForElement(element)` maps `Transition` to the future `STATE_MACHINE_TRANSITION_EDGE_TYPE`. (c) `acceptedElementKinds: ['StateUsage', 'StateDefinition']` — reserving `StateDefinition` keeps the door open for a future "called state machine" frame without an ADR revision (parallels Activity reserving `ActionDefinition`). (d) **First viewpoint with `acceptedEdgeElementKinds` set without `acceptedEdgeKinds`** — Transitions are elements-as-edge per ADR 0003 § 3 / ADR 0002, unlike Activity (Phase 5) which keeps ControlFlow/ObjectFlow in `ModelEdge`. Diff from ADR 0005 § 3 is the key Phase 6 distinction. (e) **ADR 0006** authored covering five shape decisions: free-form scope, pseudostates as `stateType` tags, Transition as element-as-edge, endpoint typing (initial cannot be target / final cannot be source / no self-loops / both endpoints must be StateUsage), and **no onConnect discrimination** (single transition kind, no shift-modifier branch needed in `CanvasPane.onConnect`). (f) New unit test `tests/unit/viewpoints/stateMachine.test.ts` (10 tests mirroring `activity.test.ts`'s shape contract — id/label, palette items, frozen-record stability, `nodeSizeFor` per-stateType, registry round-trip, `nodeTypeFor` mapping, throwing paths for non-StateUsage / non-Transition). (g) New e2e spec `tests/e2e/state-machine-empty.spec.ts` (tab+canvas mount; `@a11y` axe scan with `document.getAnimations()`-wait per the 2026-05-12 transition-flap lesson; `@visual` baseline `state-machine-empty.{chromium,webkit}.png`). (h) Visual baselines generated via `scripts/regen-baselines.sh` in the Linux container; per iteration 29 regen-script procedure reverted 29 unintended baseline rewrites (arm64↔amd64 hinting drift across pre-existing Activity / IBD / Requirements / cross-diagram-trace baselines). (i) Local `pnpm run check` green (typecheck + lint + 451/451 unit + build + 2/2 state-machine-empty chromium e2e). Auto-merge to be enabled on push.

## Next action
**Push #105 branch, open PR, enable auto-merge, and watch the first CI run.** Visual baselines for `state-machine-with-state`, `state-machine-with-pseudostates`, and `inspector-state-selected` (× {chromium, webkit} = 6 files) will be missing on first run — Playwright will write `*-actual.png` files and the test will fail. Apply the iter-39 first-time-baseline extraction procedure: download `playwright-report` from the failing run, decode the embedded `index.html` base64 zip to find the per-test `attachments[].sha1` map, copy the six actuals under `tests/e2e/__screenshots__/state-machine-nodes.spec.ts/` and commit. The `state-machine-empty.{chromium,webkit}.png` baselines from #104 (chromium/webkit) will also likely fail because of the new `+ State` toolbar button and the chip-strip palette pushing canvas chrome (iter-37 lesson); refresh via the same procedure from the second-attempt run if so. Once #105 merges, pick #106 (Transition element-as-edge + `isValidStateMachineConnection` + inspector TransitionExtras) — sequencing per the iter-41 decomposition. Recap iter-22's IBD #51 element-as-edge lessons before starting (PortUsage-as-handle pattern; `isConnectingRef` 100ms timeout against RF v12's spurious post-onConnect select-true; isValidConnection enforcement for endpoint typing — initial cannot be target, final cannot be source, no self-loops per ADR 0006 § 4).
