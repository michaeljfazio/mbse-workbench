# BDD — Block Definition Diagram

## TBD — populated by a research subagent before the first BDD deep-dive walk

Per A.9, this file answers (with citations from the source priority list in `../README.md`):

- OMG SysML v2 standard graphical notation for BDD.
- Standard relationship set (composition, aggregation, generalization, association, dependency) and the visual representation of each.
- Standard node shapes, line styles, decoration conventions.
- Common non-standard but widely-adopted conventions (Cameo / Capella / MagicDraw / Papyrus).
- Typical layout conventions (e.g. composition arrows pointing to whole; top-down hierarchy).
- Common pitfalls and reader-expectation traps.

Append-only with dated sections; older sections are not deleted, only marked superseded.
