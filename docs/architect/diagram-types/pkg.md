# Package Diagram

## TBD — populated by a research subagent before the first Package deep-dive walk

Per A.9, this file answers (with citations from the source priority list in `../README.md`):

- OMG SysML v2 standard graphical notation for Package diagrams.
- Package shape (folder/tabbed rectangle); namespace organisation conventions.
- `import` directive visualisation between packages.
- Package merge if supported.
- Containment notation (nested packages or composition-style).
- Common non-standard but widely-adopted conventions (Cameo / Capella / MagicDraw / Papyrus).
- Common pitfalls and reader-expectation traps.

Append-only with dated sections; older sections are not deleted, only marked superseded.
