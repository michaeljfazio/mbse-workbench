# Phase 15 in-flight claim board

Per A.8, this is the live registry of in-flight `phase-15/*` branches. Read before dispatching a parallel subagent — refuse to dispatch if the new branch's touched-files set overlaps with an open branch's. Soft cap: 5 in-flight branches.

When a branch merges (or is abandoned), remove its row.

| Branch | Lead engineer (subagent) | Theme | Issues | Files touched (paths) | Started | Status |
|--------|--------------------------|-------|--------|-----------------------|---------|--------|
| `phase-15/dim17-edge-reconnect-fixups` | engineer subagent (iter-896 fix-up) | Code-review fix-up for the merged #568 (`reconnectEdge` for #562). Blocker 1: route Generalization/Association ModelEdges through the correct validator by active-diagram viewpoint (UseCase Generalization is a `ModelEdge` of kind `Generalization`, same shape as BDD's, so without viewpoint disambiguation valid Actor↔Actor reconnects were silently rejected by the BDD validator). Blocker 2: when `newHandleId == null` on `ConnectionUsage`/`ItemFlow` (port-based), the action now returns `false` instead of bypassing the validator. Important 1: `end`-detection compares both node id AND handle id so same-node handle swaps on IBD ports are classified correctly. Also fixes a transposed `sourceHandle`/`targetHandle` in the IBD synthetic-connection builder discovered while writing the new tests. Test additions: ConnectionUsage + ItemFlow direct unit tests (success, invalid-port, null-handle, same-node handle swap, undo restore). Undo assertions rewritten to fail loud rather than silently pass on `undefined`. | refs #562 (already closed by #568) | `src/workspace/store.ts`, `src/workspace/__tests__/reconnectEdge.test.ts`, `docs/architect/in-flight.md` | 2026-05-19 | Branch live; PR opens this iteration. |
