# Phase 15 in-flight claim board

Per A.8, this is the live registry of in-flight `phase-15/*` branches. Read before dispatching a parallel subagent — refuse to dispatch if the new branch's touched-files set overlaps with an open branch's. Soft cap: 5 in-flight branches.

When a branch merges (or is abandoned), remove its row.

| Branch | Lead engineer (subagent) | Theme | Issues | Files touched (paths) | Started | Status |
|--------|--------------------------|-------|--------|-----------------------|---------|--------|
| `phase-15/dim17-inspector-edge-style-pickers` | engineer subagent | Close #564 + #566 — per-edge routing-style picker (straight/step/smooth-step/bezier) and stroke-style/color picker in the Inspector; `routingStyle`, `strokeStyle`, `strokeColor` on `EdgeBase` + element-as-edge elements; `setEdgeRoutingStyle`, `setEdgeStrokeStyle`, `setEdgeStrokeColor` store actions with undo; shared `edgePath.ts` helper; all 17 edge components updated; Inspector panels for all 12 ModelEdge kinds + 3 element-as-edge extras; 27 unit tests; 2 JSON round-trip tests; 2 e2e specs. | closes #564, closes #566 | `src/model/edges.ts`, `src/model/elements.ts`, `src/model/registry.ts`, `src/model/index.ts`, `src/viewpoints/shared/edgePath.ts`, `src/viewpoints/**/*(Edge|index).ts(x)` (17 files), `src/workspace/store.ts`, `src/workspace/flowGraph.ts`, `src/workspace/inspector/Inspector.tsx`, `src/workspace/__tests__/setEdgeRoutingStyle.test.ts`, `src/workspace/__tests__/setEdgeStrokeStyle.test.ts`, `src/workspace/__tests__/setEdgeStrokeColor.test.ts`, `src/workspace/__tests__/jsonProject.test.ts`, `tests/e2e/edge-routing-style.spec.ts`, `tests/e2e/edge-stroke-style.spec.ts`, `docs/architect/in-flight.md` | 2026-05-19 | PR #571 open; rebased onto main (`9872a04`) at iter-898 to clear DIRTY; conflict-marker fix in iter-898 follow-up commit; auto-merge SQUASH re-enabled after CI re-runs. |
