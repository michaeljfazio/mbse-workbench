# Phase 15 in-flight claim board

Per A.8, this is the live registry of in-flight `phase-15/*` branches. Read before dispatching a parallel subagent — refuse to dispatch if the new branch's touched-files set overlaps with an open branch's. Soft cap: 5 in-flight branches.

When a branch merges (or is abandoned), remove its row.

| Branch | Lead engineer (subagent) | Theme | Issues | Files touched (paths) | Started | Status |
|--------|--------------------------|-------|--------|-----------------------|---------|--------|
| `phase-15/walk-36-execute` | main agent | Iter-895 execute (architect hat) for walk-36 — dedicated dim-17 edge-editing deep-dive against `vphase-15.10` / `v1.6.1` Pages (re-verified byte-identical headers at driver launch). Driver: `artifacts/phase-15/walk-36/walk-36-exec.py` (gitignored). Outcome: 2 PASS (PC7 + PC8) / 18 FAIL (BDD + IBD + Activity × 6 affordances) / 0 INFO — lands on row 3 of walk-36.md § Acceptance / rubric impact exactly as the pre-plan-seal code scan predicted. Five `type:feature` issues filed: #562 (PC1 reconnect, p1, area:cross-cutting+interaction), #563 (PC2/PC3 waypoint editing, p2, area:routing+interaction), #564 (PC4 routing-style picker, p2, area:routing+inspector), #565 (PC5 label drag, p2, area:routing+interaction), #566 (PC6 stroke/color picker STRICT, p2, area:cross-cutting+inspector). Chain resets 1 → 0 / 3. Dim 17 stays at 2 with explicit PC-level evidence + promote pre-requisites. Doc-only PR (no `src/` changes); CI fast-path per ADR 0016. | (closes none — engineer batches close #562–#566; this PR is architect-hat findings) | `docs/architect/walks/walk-36.md` (§ Execution + § Decide next + § Close-out), `docs/architect/quality-rubric.md` (dim-17 evidence), `docs/architect/in-flight.md`, `STATUS.md`, `JOURNAL.md` | 2026-05-19 | Branch live; PR pending. |
